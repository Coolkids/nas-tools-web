# NAStool Web UI / UE 设计规范

> 编制日期：2026-09-13。状态：实施中。适用：Vue 3 + Quasar 响应式 Web 重构。本文是持续执行的设计规范，尚未迁移的页面仍按本规范实施；开发顺序和验收安排见 [task.md](./task.md)。

## 1. 设计方向

设计定位为“安静、清晰的家庭媒体管理中心”：探索页面以海报内容为视觉主体，管理页面以信息层次和操作效率为主体。使用暖冷适中的浅灰背景、白色层面、蓝色主操作、克制的圆角和阴影，避免将所有模块装入同样突出的彩色大卡片。

用户应能迅速回答三个问题：当前是什么状态、我能做什么、操作后发生了什么。桌面提供并排比较、批量选择和键盘路径；手机重新安排信息优先级，让搜索、订阅、下载状态和确认操作在手指可达范围内。

首版默认跟随系统主题，可切换浅色/深色并记住偏好。中文为默认界面语言；TMDB、RSS、NAS、资源格式等必要术语保留，状态和操作使用易理解的中文。

## 2. 信息架构与导航

### 2.1 导航归属

桌面主导航分为：**概览、探索、订阅、下载、整理、服务与工具、设置**。全局搜索为顶栏入口，搜索页在侧栏另有快捷入口。设置按“通用、下载与搜索、媒体与目录、识别与规则、扩展服务、用户与权限”分组；分组不需要更改已有 URL。

手机底部固定五个入口：**概览、探索、订阅、下载、更多**。探索、订阅、下载分别默认进入 `/recommend`、`/movie_rss`、`/downloading`，记住当前会话中最后使用的子页。更多承载整理、服务、设置及账号入口。更多为导航面板，记录界面历史状态；点击浏览器返回先关闭面板，选择子项后关闭并进入对应旧路由。

| 内容 | 桌面 | 手机 |
| --- | --- | --- |
| 全局搜索 | 顶栏常驻输入框，支持 Enter 提交 | 顶栏搜索按钮进入搜索页，搜索页输入框置顶 |
| 探索子分类 | 页内来源/类型 Tab，侧栏保留探索分组 | 少量主 Tab + 来源筛选；不堆叠两排横向菜单 |
| 订阅子页 | 电影、剧集、日历、历史、自定义入口 | 电影/剧集主 Tab；日历和更多功能用明确按钮进入 |
| 整理与设置 | 侧栏分组 | 从更多进入分组页/面板，再选子项；功能完整保留 |
| 消息/日志 | 顶栏消息按钮、工具入口 | 顶栏消息按钮，日志在更多 → 服务与工具 |
| 详情页归属 | 保留来源导航高亮 | 探索详情高亮探索；从订阅/下载进入时保留来源归属 |

导航数据和权限映射只有一份。列表状态与页面内 Tab 不因切换设备尺寸而重置。设置与整理等子页高亮“更多”；底部导航只显示数量有意义的徽标，不显示所有模块的数字。

### 2.2 返回与层级

- 主入口切换使用正常路由历史；再次点击当前入口不自动清空筛选或滚回顶部。
- 详情页有“返回”按钮；有来源时返回原列表并恢复位置，没有来源时回所属模块默认页。
- 只读轻量弹层由关闭按钮、Esc 或遮罩关闭；重要表单有未保存检查。
- 手机返回顺序：当前辅助面板 → 当前表单/详情层 → 来源页。必须通过历史状态或路由协调实现，不能假设 QDialog 会自动处理浏览器历史。
- 登录成功回到原应用内目标；会话失效不把用户带入反复跳转。没有权限显示说明和返回入口。

## 3. 响应式布局与输入方式

### 3.1 统一断点

使用 Quasar 默认断点作为唯一全局基础：`xs < 600`、`sm 600–1023`、`md 1024–1439`、`lg 1440–1919`、`xl ≥ 1920`，单位为 CSS px。[Quasar 断点文档](https://quasar.dev/style/breakpoints/)

| 宽度 | 应用外壳 | 页面内容 | 操作形式 |
| --- | --- | --- | --- |
| `< 600` | 56px 顶栏；56px 底部导航，另加设备安全区 | 单列为主，左右 16px；320px 兜底可降至 12px | 卡片/列表、底部短面板、全屏长表单 |
| `600–1023` | 56px 顶栏；菜单按钮打开覆盖式导航；不显示手机底栏 | 左右 24px；按容器空间一至两列 | 表单可双列；复杂列表优先卡片，足够宽才用表格 |
| `1024–1439` | 64px 顶栏；72px 迷你侧栏，可展开为覆盖式 240px 菜单 | 左右 24px | 标准表格、独立详情页、居中表单 |
| `≥ 1440` | 64px 顶栏；240px 常驻侧栏，可折叠至 72px | 左右 32px，主内容最大 1600px 居中 | 高效表格、必要的双栏/主从布局 |

只需重排样式时优先 CSS；确需切换组件表现时才使用 Screen/composable。改变 Screen 的 JS 配置不会自动修改 CSS 断点，因此本项目不各自定义不一致的断点。[Screen 插件说明](https://quasar.dev/options/screen-plugin/)

窗口断点决定外壳，组件容器宽度决定内部密度。例如桌面半屏详情内的列表也可能使用卡片。避免在侧栏挤压后仍强行保留全部列。

### 3.2 尺寸与滚动

- 页面统一由布局容器提供外边距，业务页面不重复添加一层 16/24px padding。
- 普通阅读页面使用单一主滚动区；桌面大表和日志可有局部滚动，但应有明确边界。
- 海报网格按可用宽度自适应：手机至少 2 列，平板通常 3–5 列，桌面通常 5–8 列；卡宽约 136–184px，极窄手机允许降至 128px。
- 设置表单阅读区最大宽 960px，常规详情正文最大宽 1120px，不在超宽屏拉成长行。
- 手机底部固定操作与底部导航不叠放：长表单/全屏流程暂时隐藏导航，由表单操作栏占据底部；普通详情的操作栏置于导航上方并给正文预留两者高度。
- 安全区统一处理 `env(safe-area-inset-*)`，使用动态视口高度方案并提供回退；输入聚焦后字段、错误和提交按钮可滚入可见区域。
- 不全局禁用浏览器缩放；不得通过隐藏 overflow 掩盖被裁掉的内容。

### 3.3 鼠标、键盘与触屏

- 交互区域至少 44×44px，手机主要按钮和表单输入高度 48px。紧凑视觉图标可以小，但点击区域不能缩小。
- 鼠标悬停只增加提示或层次，不承担唯一入口；卡片操作在键盘聚焦和触屏下同样可用。
- 卡片主体进入详情，内部按钮执行独立动作并阻止冒泡，避免点击下载同时进入详情；不使用嵌套按钮。
- 提供显式“选择”按钮进入手机批量模式，长按可作增强但不是唯一途径；删除不用滑动即执行。
- 支持 Tab/Shift+Tab、Enter/Space，Esc 关闭可关闭的层；输入框内 Enter 遵守当前表单语义，多行文本不误提交。

## 4. 视觉系统

以下 token 是本项目设计值，开发时映射到 Quasar 主题变量与 CSS 语义变量。颜色不能代替文本和图标。

### 4.1 色彩

| 语义 token | 浅色 | 深色 | 用途 |
| --- | --- | --- | --- |
| `page-bg` | `#F5F7FB` | `#10141D` | 页面背景 |
| `surface` | `#FFFFFF` | `#191F2B` | 卡片、导航、表单 |
| `surface-raised` | `#FFFFFF` | `#222B3A` | 对话框、菜单 |
| `surface-muted` | `#EDF1F7` | `#252E3D` | 次级区域、骨架 |
| `text-primary` | `#172033` | `#F1F5FB` | 正文、标题 |
| `text-secondary` | `#526079` | `#B5C0D2` | 辅助信息 |
| `border-subtle` | `#DDE3ED` | `#394457` | 装饰性分隔 |
| `control-border` | `#7A879B` | `#8290A7` | 必须辨识的输入框边界 |
| `primary` | `#315BD6` | `#9CB7FF` | 选中、主要操作、链接 |
| `on-primary` | `#FFFFFF` | `#101A32` | 主按钮文字/图标 |
| `primary-soft` | `#EAF0FF` | `#263B64` | 选中背景 |
| `positive` | `#187348` | `#73D7A5` | 成功/完成 |
| `warning` | `#8A5700` | `#F0C36B` | 注意/待处理 |
| `negative` | `#BE303A` | `#FF9AA3` | 错误/危险动作 |
| `info` | `#176B9A` | `#80CCF5` | 说明 |

状态标签默认“浅背景 + 深文字”或深色对应配色；填充状态按钮需单独定义文字颜色。必须检查 Quasar 默认白字按钮在深色 primary 下的对比度，不能仅覆盖 primary 色值。

### 4.2 字体与排版

- 使用本地系统字体栈：`-apple-system, BlinkMacSystemFont, "Segoe UI", "PingFang SC", "Microsoft YaHei", sans-serif`，不依赖在线字体加载。
- 页标题：桌面 28/36px、手机 24/32px、字重 600；分区标题 18/26px、600；卡片标题 16/24px、600。
- 正文：桌面 14/22px、手机 16/24px；辅助文字 12–13/18–20px。手机输入文字至少 16px。
- 统计数字 28–32px，最多展示必要精度；时间、大小、速度使用等宽数字对齐。日志用等宽字体，默认至少 13px。
- 长标题默认显示两行，种子标题可显示三行并有“展开”；完整值可在详情查看/复制，不能仅靠 tooltip。
- 空数据用“—”并解释原因；数据未获取或失败不显示为 0。

### 4.3 间距、圆角和层次

| 项目 | 规范 |
| --- | --- |
| 间距刻度 | 4、8、12、16、24、32、48px；字段间 16px，分组间 24–32px |
| 圆角 | 输入/按钮 8px，卡片 12px，对话框/底部面板 16px，徽标可胶囊形 |
| 卡片 | 以 1px 细边框为主，管理页默认无阴影；避免多层嵌套卡片 |
| 浮层阴影 | 浅色可用 `0 8px 24px rgba(23,32,51,.12)`；深色以边界和层面色区分 |
| 分隔 | 同组用留白，跨组用标题/分隔线，不对每个字段套框 |
| 图标 | 单一 Material Symbols Outlined 风格或等价本地图标子集；通常 20/24px |

海报承担视觉丰富度，管理页面不放无功能的大幅横幅。媒体详情可使用局部背景图和渐变遮罩，但标题/按钮始终位于对比度可靠的层面上。

### 4.4 动效

按钮/悬停反馈 120–160ms，展开/面板切换 180–240ms，以透明度和小范围位移为主。状态更新不让整个列表跳动；骨架与真实内容尺寸尽量一致。尊重 `prefers-reduced-motion`，关闭非必要位移和循环装饰动画。

## 5. 通用组件与交互规范

### 5.1 页头与按钮

页头依次为标题、必要说明、当前状态/数量、主操作与次操作。每个操作区域最多一个实心主按钮；刷新、筛选等使用次级按钮，危险动作置于更多菜单或独立危险区域。

文案采用“动词 + 对象”，如“添加订阅”“保存下载器”“删除 3 个文件”。提交期间显示进行中并禁止重复提交；只有接口确认成功后显示成功状态。取消始终代表放弃当前操作，不能像现有搜索订阅类型选择那样把“取消”映射为另一种业务类型。

### 5.2 媒体卡片

- 海报固定 2:3，预留尺寸并懒加载；失败显示统一占位和标题，不出现破图或高度塌陷。
- 海报下方为两行标题、年份/类型；评分放小型角标，订阅/已下载状态用带文字的标签，与评分位置分开。
- 电影、电视剧、动漫在同一套组件上通过数据表达；人物使用另一种头像/姓名卡片，不混用媒体状态。
- 手机整卡进入详情，通过详情完成完整订阅；需要快捷订阅时用常显菜单或按钮，不在海报上堆满四个悬浮图标。
- 榜单横向内容带“查看全部”；桌面有上一组/下一组按钮，手机可滑动，任何项目都能通过完整列表访问。

### 5.3 表格、列表与筛选

桌面管理表使用 QTable，固定表头按需要启用；保留主要信息列和操作列，低频字段进入展开区。纯数据行默认 48px，媒体任务行约 72px；紧凑模式仅在桌面鼠标场景作为可选偏好。

手机使用摘要卡片/QList：标题 → 状态 → 两至三项决策数据 → 主操作/更多。其余字段在详情或展开区域可达；不把所有表格列直接竖排成一张超长卡片。

筛选规则：

- 高频筛选在页面直接可见；完整筛选桌面右侧面板，手机底部面板或全屏。
- 面板内修改为草稿，“应用”才生效，“重置”清空面板条件，“取消”保持原条件。
- 应用后显示筛选摘要与数量，提供逐个移除和“清除筛选”；搜索服务端任务需显式提交，避免输入每个字符都创建任务。
- 本地文字过滤可约 300ms 防抖；标明当前结果与数量。切换筛选回到第一页。
- 服务端有分页时按其契约请求；无分页时使用前端分页/虚拟列表，不能宣称减少了网络数据量。
- 手机默认“加载更多”或简化分页，保留已加载项与返回锚点；桌面用分页器。加载失败只在列表末尾提供重试。
- 批量选择默认限定当前已加载结果，明确数量与范围；更换查询条件清空选择并提示，不能暗中跨筛选操作。

### 5.4 弹层和表单

| 场景 | 桌面 | 手机 |
| --- | --- | --- |
| 删除等短确认 | 居中 400–480px，受视口约束 | 视口内确认框，左右 16px 留白 |
| 短选项、排序、轻量筛选 | QMenu 或小型 QDialog | 底部面板，最高约 70dvh，内部可滚动 |
| 新增下载等中等表单 | 560–720px QDialog | 全屏表单，顶栏标题/关闭，底部提交 |
| 订阅高级参数、文件转移、复杂设置 | 720–960px 表单/独立页 | 全屏，按分组渐进展开 |
| 媒体/搜索结果详情 | 独立页或可恢复主从区 | 独立页/全屏详情，保留返回来源 |

QDialog 支持全屏等展示能力，但容器尺寸、表单草稿和应用返回逻辑由项目统一封装。[QDialog 官方文档](https://quasar.dev/vue-components/dialog/)

- 标签默认置顶，不用 placeholder 代替标签；必填有标识，格式/单位说明贴近字段。
- 同一任务先展示必要参数，“高级设置”收纳低频字段，展开后保存原值。复杂表单按业务分组，不按技术配置键分组。
- 首次输入时不立即满屏报错；失焦或提交后校验，提交失败定位第一个错误并保留全部输入。
- 新增和编辑使用相同字段布局，但标题、默认值和提交文案明确区分。
- 设置修改先进入草稿，明确“保存本组/保存此配置”的范围；“测试连接”与“保存”是两个独立动作。
- 密码默认遮挡并有显示按钮；后端返回脱敏占位时不把占位保存回去，“未修改”和“清空”必须分开定义。
- 避免第二层完整对话框覆盖第一层；选季、媒体匹配等子流程采用同层步骤或替换内容，并提供返回表单入口。
- 有未保存修改时离开：提示“继续编辑 / 放弃修改”，不假装已自动保存；切换宽度/横竖屏不触发丢弃。

### 5.5 状态与反馈

| 状态 | 必须呈现 |
| --- | --- |
| 首次加载 | 与最终布局一致的骨架；有明确区域标题 |
| 后台刷新 | 保留当前内容，小型更新标识，不整页闪白 |
| 尚无数据 | 原因 + 下一步，如“还没有订阅”与“添加订阅” |
| 筛选无结果 | 展示筛选上下文 + 清除筛选，不误导去创建数据 |
| 局部加载失败 | 对应区域内错误与重试，其他模块继续使用 |
| 网络中断 | 横幅说明，保留最近数据与更新时间，恢复后重新拉取 |
| 未登录/无权限 | 分开处理，提供登录/返回入口，保留合理目标 |
| 提交中 | 当前按钮 loading、阻止重复操作，其他安全区域可用 |
| 提交成功 | 简短 Toast + 相关数据更新；必要时提供“查看任务” |
| 提交失败/结果未知 | 表单或条目保留，说明失败原因或需核对结果，不静默重复提交 |

Toast 桌面右上，手机位于底栏/操作栏上方；成功通常 2–3 秒，错误较长并有可关闭/查看详情入口。错误详情留在所属页面，不能只依赖即逝消息。相同轮询错误合并，不每轮弹一次。

未知后台进度使用“处理中”和不定进度条；只有接口提供可靠数值时显示百分比。上传字节进度必须标为上传，不代表媒体整理或下载任务已完成。

### 5.6 危险操作

- 删除按钮与常用暂停/编辑动作保持距离，手机不在每张卡片上突出红色主按钮。
- 确认内容写明对象、数量、影响及不可恢复性；批量动作可展开目标清单。默认焦点放在安全动作。
- 下载删除若会删除文件，按钮写“删除任务及文件”，正文明确影响；只有接口支持时才提供保留文件的独立选项。
- 文件移动、历史恢复、缓存清空等按实际语义描述。接口无撤销能力时不展示“撤销”Toast。
- 批量部分失败显示成功/失败数量及失败对象，允许只处理失败项；超时结果未知时先核对状态。

## 6. 主要页面设计

### 6.1 概览

首屏突出系统当前状态和继续操作入口，信息顺序：需要关注的异常 → 媒体/空间摘要 → 下载摘要与快捷入口 → 转移统计与播放历史。增加摘要需使用已存在、经验证的接口，按需加载，不为了首页数字遍历全部配置或日历。

桌面采用 12 栏网格：统计摘要四等分，空间/趋势或任务摘要按 8:4 分布，近期内容在下方。手机统计两列，异常、任务摘要、空间、近期内容纵向排列，趋势详情默认收起或后置。服务器未配置提供配置入口；连接失败提供重试，不能把两者混为一谈。

### 6.2 搜索与资源结果

搜索由“搜索输入 → 任务列表 → 当前任务结果 → 选择下载参数”组成。普通搜索入口醒目，高级选项包含现有类型、年份、季、资源类型、分辨率、促销和规则条件。

桌面在空间足够时使用任务列表与结果主从布局，当前任务标题和状态始终可见；不足时改为列表进入结果页。手机任务列表进入单独结果视图，顶部显示关键词、状态和返回，不用宽表弹窗。

资源结果的信息优先级：

1. 种子/资源名称，必要时可展开完整值。
2. 站点、大小、做种数，以及接口实际提供的免费/促销标记。
3. 分辨率、编码等仅在有可靠字段时展示，不从未验证数据推断。
4. “下载”主动作，“查看站点”次动作；下载前确认目录和下载设置。

搜索状态明确为排队、搜索中、完成、失败。尚未完成时不显示“没有资源”；切换任务后旧请求不得覆盖新任务内容。删除搜索任务说明删除的是搜索记录及结果，不应暗示删除下载任务。

### 6.3 探索、媒体与人物详情

探索以海报网格/少量榜单条带为主，来源和媒体类型清晰，避免一次加载所有远程榜单。显示来源失败与图片失败的局部回退。

媒体详情桌面为海报与信息并排，下方简介、季集、演职员和推荐；手机先展示标题、年份/类型、评分和主动作，再展示简介和其他信息，海报不能占满首屏。

详情主操作为“添加订阅”，次操作为“搜索资源”；已订阅时提供“查看/编辑订阅”，不要重复引导新增。电视剧订阅需要清楚展示季、集范围和已选摘要。人物页展示人物身份、作品分类及来源信息，返回保留作品列表位置。

### 6.4 订阅管理与日历

桌面订阅支持海报卡片与管理列表的合理切换，手机以小海报摘要列表为主。每项显示名称、电影/剧集、季集信息、当前状态和规则摘要；低频站点配置进入详情，避免标签铺满整卡。

新增订阅流程：选择媒体和类型 → 剧集选择季/集 → 基础订阅设置 → 按需展开站点/规则等高级设置 → 确认摘要并保存。基础和高级共用同一草稿；返回上一步保留输入。

电影/剧集日历桌面显示月历，点击日期查看当日清单；手机默认日程列表，上方日期条和“今天”按钮，可切换月份。区分日期与具体时刻，明确所用时区；没有播出时刻时只显示日期，不编造午夜时间。事件进入对应媒体或订阅详情。

自定义 RSS、解析器与订阅历史使用管理列表模板，保留测试、运行、条目下载、重新订阅等操作。运行后显示已提交/执行中等真实状态，不把请求受理当成完成。

### 6.5 下载管理

桌面显示任务、进度、状态、速度及操作，按可用字段扩展。手机每项为标题、状态、线性进度、速度、开始/暂停与更多菜单。原始种子名称可展开。

状态使用后端状态映射；未知状态显示“未知状态”及可读原值，不把所有非 Downloading 状态都标成“已暂停”。正在控制的任务按钮进入等待态，响应确认后更新，失败保留原状态。

“新增下载”提供“种子文件 / 磁力链接”显式类型选择。桌面支持拖放并提供文件按钮；手机调用文件选择器，磁力链接支持粘贴。展示已选文件、验证结果、目录与下载设置，上传失败能替换/移除文件后重试。手机系统不支持某个文件来源时仍能使用磁力入口。

### 6.6 文件管理与识别转移

桌面使用目录树/当前位置 + 文件表格 + 必要的详情区；手机使用逐级目录列表、上一级按钮和可展开完整路径的面包屑。目录单击进入，文件单击选择/看详情，动作菜单与导航行为分开。

批量模式显示选择数量，允许退出；切换目录默认清空选择并提示。路径可复制，不让很长的路径撑破屏幕。字幕、重命名、硬链接查询放入相应文件动作或工具入口。

识别转移分为：选择来源 → 确认媒体/季集 → 目标目录与转移方式 → 核对并执行。核对区展示用户选定参数；只有后端提供逐文件预览时才展示精确的新文件名和冲突判断，否则注明“实际结果以执行结果为准”。复制、移动、硬链接等用短说明解释影响，不替用户隐藏现有方式。

### 6.7 服务、设置与账号

服务分“定时服务”“手动工具”“清理操作”，每项显示名称、配置状态、调度信息和运行入口。配置启用状态与当前执行状态分开；日志入口靠近相关任务，执行成功提示提供查看入口。

设置桌面为分组导航 + 表单，手机为分组列表 → 单组编辑。组内上方说明用途，先基础参数后高级参数；新增下载器等连接配置显示测试按钮与测试结果，测试不自动保存。

规则/识别词等多行配置提供示例说明、导入内容检查和错误定位；手机使用可滚动文本区/全屏编辑，保留现有规则能力。用户管理对权限使用带文字的分组勾选，不展示无说明的内部权限码。

登录页采用简洁品牌标识与单列表单，桌面表单宽约 400px，手机左右 24px；提供密码显示、记住登录和提交状态。键盘弹出或横屏时仍可滚动至提交按钮，不用固定垂直居中裁掉内容。

### 6.8 消息与实时日志

消息桌面从顶栏打开侧面内容，手机全屏；显示时间、类型、正文和已有可用操作。未读状态、已读操作只在有后端或明确定义的本地状态支持时提供。

日志桌面可用大面板，手机全屏；提供级别/来源筛选（以接口能力为准）、暂停刷新、换行与复制。用户向上滚动时停止自动贴底，并显示“有新日志 / 回到底部”；日志最多保留约 1,000 条可配置记录，使用虚拟列表或受控截断。日志内容默认作为文本呈现。

## 7. 结构线框示例

下列是布局和操作优先级示意，示例数据不代表真实后端状态。视觉稿需补充颜色、间距、状态和深色变体。

### 7.1 桌面搜索结果（宽屏）

```text
┌────────────┬──────────────────────────────────────────────────┐
│ NAStool    │ 搜索名称、年份…                         消息  账号 │
│ 概览       ├──────────────────────────────────────────────────┤
│ 探索       │ 资源搜索                         [高级搜索] [搜索]│
│ 订阅       ├──────────────┬───────────────────────────────────┤
│ 下载       │ 搜索任务     │ 示例剧集 · 已完成 · 128 个结果     │
│ 整理       │ ● 示例剧集   │ [站点] [排序] [筛选 2] [清除]     │
│ 服务与工具 │ ○ 另一任务   ├───────────────────────────────────┤
│ 设置       │              │ 名称          站点 大小 做种 操作 │
│            │              │ …             …    …    … [下载]│
│ 搜索       │              │ …             …    …    … [下载]│
│            │              ├───────────────────────────────────┤
│            │              │ 结果范围                   分页   │
└────────────┴──────────────┴───────────────────────────────────┘
```

### 7.2 手机搜索结果与下载确认

```text
┌──────────────────────┐     ┌──────────────────────┐
│ ‹ 示例剧集      搜索  │     │ 关闭      添加下载    │
│ 已完成 · 128 个结果   │     │ 示例资源完整名称…    │
│ [站点] [排序] [筛选2] │     │ 站点 · 大小          │
├──────────────────────┤     │                      │
│ 示例资源名称，最多   │     │ 下载设置             │
│ 三行，可展开         │     │ [选择设置          ▾]│
│ 站点 · 12 GB · 36做种│     │ 保存目录             │
│ [免费]  [下载] [更多]│ ──→ │ [选择目录          ▾]│
├──────────────────────┤     │                      │
│ 下一项资源…          │     │ 说明/校验错误区域    │
│       [加载更多]     │     ├──────────────────────┤
├──────────────────────┤     │ [取消]     [添加下载]│
│概览 探索 订阅 下载 更多│     │      系统安全区      │
└──────────────────────┘     └──────────────────────┘
```

确认表单占据全屏并替换底部导航；成功后回结果位置，提供“查看下载任务”。

### 7.3 手机下载与设置编辑

```text
┌──────────────────────┐     ┌──────────────────────┐
│ 下载       刷新  ＋  │     │ ‹       下载器设置   │
│ [任务] [最近下载]    │     │ 基础连接             │
├──────────────────────┤     │ 名称                 │
│ 示例媒体 · 第 1 季   │     │ [家庭下载器         ]│
│ 下载中               │     │ 地址                 │
│ ███████░░░░░  62%    │     │ [服务器地址         ]│
│ 8.2 MB/s             │     │ 用户名 / 密码…       │
│ [暂停]       [更多]  │     │                      │
├──────────────────────┤     │ [测试连接]  测试结果 │
│ 下一项任务…          │     │ ▸ 高级设置           │
│                      │     │ 未保存修改           │
├──────────────────────┤     ├──────────────────────┤
│概览 探索 订阅 下载 更多│     │ [取消]   [保存下载器]│
└──────────────────────┘     └──────────────────────┘
```

## 8. 无障碍与可用性

以 WCAG 2.2 AA 为验收目标：普通文字对比度至少 4.5:1，大字至少 3:1，必要控件边界/焦点等非文本信息至少 3:1；本项目进一步采用至少 44×44px 的主要触控目标。验收需实际测量，不能仅凭色值判断已合规。[W3C WCAG 2.2 快速参考](https://www.w3.org/WAI/WCAG22/quickref/)

- 每页一个主要标题，语义化 main/nav；按钮、输入、图标动作有可访问名称。
- 弹窗打开后将焦点放入合理位置并限制在当前模态内，关闭后回到触发控件；表单出错聚焦首个错误。
- QDrawer 的覆盖外观不保证完整模态焦点管理；按当前采用版本补齐导航抽屉的焦点、背景可访问性与关闭后恢复，并实测键盘/读屏。[QDrawer 可访问性说明](https://quasar.dev/layout/drawer/)
- 导航当前项、展开状态、选中数量和表单错误对辅助技术可感知；后台频繁进度不要每次都朗读。
- 页面缩放至 200% 后内容与功能可用；文本放大可换行，不将按钮或标签裁切。
- 图表有文本摘要或等价数据，海报提供必要替代文本，装饰图片不重复朗读标题。
- 兼顾色觉差异，成功/失败/暂停有文字或形状差异；任何动作不单靠颜色区分。

## 9. 设计交付与验收清单

### 9.1 后续视觉稿交付

至少提交 1440px 桌面与 390px 手机的概览、搜索结果、媒体详情、订阅列表/编辑、下载列表/新增、文件管理/转移、设置列表/编辑、登录界面；补充 768px 平板导航与复杂表单。

每个关键模板补齐：默认、加载、空、错误、长文本、提交中、成功、无权限以及深色状态。组件稿包括导航、按钮、表单、媒体卡、任务卡、表格、筛选、危险确认、Toast 和日志。标注尺寸、token、可点击区域、Tab 顺序、返回行为及滚动容器。

### 9.2 可直接用于评审的检查项

- [ ] 同一功能两端入口可找到，手机没有缺失设置、规则、日志等低频能力。
- [ ] 用户能在不阅读帮助的情况下完成搜索下载、添加订阅、暂停任务、识别转移和保存设置。
- [ ] 手机列表无需整页横向滚动；长标题、完整目录与低频字段有明确查看途径。
- [ ] 390px 首屏能看到页面身份、核心状态和主操作；探索图片不压过任务入口。
- [ ] 所有主要触控区域至少 44×44px，软键盘、安全区与底部操作栏不互相遮挡。
- [ ] 列表返回、过滤取消、表单返回和旋转屏幕后状态符合预期。
- [ ] 不用“取消”表达另一业务选项，不展示后端不支持的撤销/取消任务/保留文件开关。
- [ ] 网络失败与空数据区分，刷新不清空有效内容，成功反馈对应真实业务成功。
- [ ] 危险动作说明对象、范围和后果，批量部分失败可定位。
- [ ] 桌面键盘、手机触控、浅色/深色和真实设备均完成检查。
- [ ] 所有旧页面和全局弹窗遵守同一套视觉 token、状态和操作规范。

设计稿与开发实现均应关联 [task.md](./task.md) 的页面清单和阶段验收，不以完成少数示例页面作为整个双端重构完成。
