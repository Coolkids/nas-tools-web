# NAStool Web 双端统一重构计划

> 编制日期：2026-09-13。状态：实施中。本计划作为重构实施基线；当前已完成 Quasar 基础接入、应用外壳、全部路由页面首批迁移，以及全局弹窗、消息中心、实时日志等共享组件迁移。文件管理、基础设置、过滤规则、自定义识别词和手动识别均已接入桌面/移动响应式布局；后续按 P4–P6 做真实接口联调、浏览器验收和体验收口。UI/UE 实施依据见 [style.md](./style.md)。

## 1. 目标与范围

使用 **Vue 3 + TypeScript + Quasar** 重构现有前端，以一套源码、一套路由、一套业务逻辑同时支持桌面与移动浏览器。桌面侧重信息对比、键盘操作与批量管理；手机侧重单手操作、逐步完成任务和清晰反馈。两端功能权限一致，允许布局、信息密度和交互容器不同。

本计划默认交付响应式 SPA，不把“桌面端”解释为 Electron 安装包，也不把“移动端”解释为 Android/iOS 原生应用。PWA、Electron、Capacitor 打包列为后续独立事项，不作为本轮完成条件。

纳入范围：现有全部可路由页面、全局搜索、消息、日志、登录、权限呈现、媒体相关弹窗、浅色/深色主题、响应式导航、请求状态和交互一致性。保留后端接口协议和已有能力；因后端能力不足产生的新增需求单独记录，不以虚构字段或前端假状态完成验收。

## 2. 源码盘点与依据

以下是源码检查结果，不代表已经通过浏览器或真实后端验证。

| 项目 | 当前情况 | 对重构的影响 |
| --- | --- | --- |
| 框架与构建 | `package.json` 声明 Vue `^3.5.42`、Vite `^8.2.2`、TypeScript `^5.9.3`、Vue Router `^5.3.1`、Pinia `^4.0.3` | 延续现有框架，不按 README 的旧版依赖说明实施 |
| UI 依赖 | Quasar 已在 `src/main.ts` 接入并承担页面 UI；Element Plus、Element 图标和 VXE Table 已从源码、package.json 与锁文件移除 | 统一使用 Quasar，桌面表格和移动卡片按页面空间切换 |
| 页面 | `src/views` 共 40 个 Vue 文件；`/ranking` 与 `/bangumi` 共用页面；另有根路径重定向 | 路由数量与文件数量不同，必须按路由和功能双重核对 |
| 布局 | `DefaultLayout.vue` 使用固定侧栏，样式定义侧栏 220px、顶栏 56px | 重建应用外壳和导航，不沿用固定桌面布局 |
| 响应式 | 登录、媒体详情、搜索、订阅弹窗等有局部媒体查询，多处使用不同断点 | 收敛断点、间距、弹窗和列表策略 |
| 请求 | `src/api/request.ts` 用 Axios；`/do` 提交表单字段 `cmd`、JSON 字符串 `data`；还有 REST、上传接口 | 保留传输层契约，逐模块提取类型与结果适配 |
| 返回结构 | 同时存在 `code/msg`、`retcode/retmsg`、登录 `success/message` | 按接口定义成功条件，不把 HTTP 200 当作业务成功 |
| 认证 | Cookie 会话；`checkAuth()` 当前通过 `get_users` 探测；路由用模块变量记录校验状态 | 验证普通用户权限、并发导航、断网和会话失效，不能把用户列表权限等同登录态 |
| 业务耦合 | 多个 view 直接调用 `doAction`；请求与页面状态仍需继续抽取 | 将业务请求和 UI 反馈分层，避免仅替换标签 |
| 实时状态 | 下载与搜索有 5 秒轮询，消息与日志有各自计时器 | 统一页面可见性、取消、去重、失败恢复和后台暂停策略 |
| 搜索 | 任务列表、结果弹窗、大表、高级筛选；顶栏与页面重复高级搜索逻辑 | 抽取共享搜索模型，结果改为可恢复的主从流程 |
| 滚动与链接 | 路由统一回到顶部；详情、人物、文件和搜索使用 query 参数 | 保留深链接并实现返回后的筛选、分页、滚动恢复 |
| 部署 | `vite.config.ts` 实际开发端口 5174，目标读取进程环境变量 `FLASK_TARGET`；README 记载的端口及部分文件与仓库不一致 | 以实际配置为准，后续补齐部署示例与文档 |

重点检查文件：`src/router/index.ts`、`src/layouts/DefaultLayout.vue`、`src/components/AppSidebar.vue`、`src/styles/index.scss`、`src/api/*`、`src/stores/*`、`src/composables/*` 及各业务页面。

当前终端 PATH 未直接解析 `node`、`npm`，本项目使用 `C:\Program Files\nodejs\npm.cmd` 执行脚本；已完成 `npm run type-check` 和 `npm run build`。本次使用临时登录会话验证了后端只读接口及 Vite 代理（首页、`/login_json`、`/do?cmd=get_users` 均成功），密码和会话值未写入仓库；后续仍需浏览器端逐页验收。

## 3. 技术决策

### 3.1 Quasar 接入方式

优先在现有 Vite 项目中接入 `quasar`、`@quasar/vite-plugin` 和按需图标资源，保留 Vue Router、Pinia、Axios、TypeScript。这样能围绕 UI 和业务分层迁移，维持现有构建入口与同源代理。

截至本次查阅，官方 Vite 插件 v2 要求 Vite 8+、`@vitejs/plugin-vue` 6+、Quasar 2.24+、Node 20.19+，并说明现代 Quasar Sass 接入使用 `sass-embedded`。仓库声明的 Vite 与 Vue 插件版本符合对应主版本要求，但仍需安装验证、核对各依赖 engine、锁定兼容组合及更新锁文件，不能直接视为构建通过。[Quasar Vite 接入文档](https://quasar.dev/start/vite-plugin/)

执行约束：

- 使用仓库现有 `package-lock.json` 对应的 npm 工作流，统一安装命令和 CI；不混入第二种锁文件。
- 保留当前 Router、Pinia 主版本，除非兼容性验证发现明确阻碍；不因旧 README 而降级。
- 配置中文语言包、统一图标集、主题变量及必要 Quasar 插件；只导入使用的图标和动画。
- 本轮不迁移 Quasar CLI；若未来需要官方整合的原生打包、SSR 等能力，再评估 CLI。Vite 插件和 Quasar CLI 的能力不同，不能把接入 UI 组件视为已经获得全部打包能力。[接入方式与限制](https://quasar.dev/start/vite-plugin/)

### 3.2 一套代码的边界

```text
同一业务路由
  └─ 业务页面：组织查询、表单和动作
      ├─ composable / store：共享状态、校验、权限和请求生命周期
      ├─ API adapter：兼容原始协议，输出领域数据及明确错误
      └─ 展示组件：按容器空间切换表格/卡片、对话框/全屏表单
```

- 不创建独立 `desktop-app`、`mobile-app` 或两份业务页面目录。
- CSS 负责排版，统一 `useResponsive` 只负责必要的交互模式切换；不依赖 User-Agent 判断宽度。
- 表格与卡片可以是两个轻量展示组件，但请求、过滤、排序、选择集、权限和动作只有一个实现；同一时刻仅挂载需要的重型视图。
- 断点切换不得重新提交、丢失草稿、重复启动轮询或清空选择。断点定义见 `style.md`。
- 不建立包办全部业务的巨大通用表单/表格配置引擎；先抽取真实重复能力。

### 3.3 建议目录演进

下列为未来组织目标，不要求一次搬迁所有文件：

```text
src/
  api/                  # 请求传输、分领域原始接口、响应适配
  types/                # 共享领域类型、权限和状态枚举
  stores/               # 会话、偏好、需要跨页恢复的业务状态
  composables/          # 查询、轮询、表单、响应式与选择集
  layouts/              # 统一 AppLayout
  navigation/           # 菜单分组、路由归属与可见权限
  components/
    app/                # 顶栏、侧栏、底部导航、消息和日志入口
    shared/             # PageHeader、AsyncState、AdaptiveDialog 等
    media/              # 海报卡片、详情、选季、订阅和下载表单
  views/                # 延续现有领域分组，不按设备拆分
  styles/               # Quasar 变量、语义 token、基础和布局样式
```

`api` 不引用 UI 组件；页面通过统一反馈服务显示消息。Pinia 不存储整个响应的无边界副本，也不持久化会话 Cookie、密码、下载器密钥或完整系统配置。

## 4. 全量页面迁移与导航归属

阶段编号与第 7 节一致。以下路径全部保留可访问，导航分组调整不等于删除页面。

| 现有路径 | 目标入口/页面形态 | 关键保留能力 | 阶段 |
| --- | --- | --- | --- |
| `/`、`/index` | 概览 | 媒体数量、磁盘、转移统计、播放历史、局部刷新 | P3 |
| `/login` | 独立登录页 | 账号密码、记住登录、登录后回原页 | P2 |
| `/search` | 全局搜索、搜索任务与结果 | 普通/高级搜索、任务状态与删除、筛选、下载、订阅 | P3 |
| `/recommend` | 探索 → 推荐 | 分类、翻页、查看详情 | P4 |
| `/ranking`、`/bangumi` | 探索 → 榜单/每日放送 | 来源分类、星期切换 | P4 |
| `/douban_movie`、`/douban_tv` | 探索 → 豆瓣 | 电影/剧集分类浏览 | P4 |
| `/tmdb_movie`、`/tmdb_tv` | 探索 → TMDB | 电影/剧集分类浏览 | P4 |
| `/media_detail`、`/discovery_person` | 媒体详情、人物详情 | 简介、演职员、关联作品、推荐、搜索/订阅 | P4 |
| `/movie_rss`、`/tv_rss` | 订阅 → 电影/剧集 | 新增、编辑、刷新、删除、规则与站点选择、季集信息 | P4 |
| `/rss_history` | 订阅 → 历史 | 类型筛选、分页、重新订阅、删除记录 | P4 |
| `/rss_calendar` | 订阅 → 日历 | 电影/剧集日程，桌面月历、手机日程列表 | P4 |
| `/user_rss`、`/rss_parser` | 订阅 → 自定义/解析器 | 任务管理、运行、条目/历史、测试、下载、解析规则 | P4 |
| `/downloading` | 下载 → 任务 | 状态刷新、开始/暂停/删除、种子上传、磁力链接 | P3 |
| `/downloaded` | 下载 → 最近下载 | 浏览已下载媒体、查看详情 | P4 |
| `/torrent_remove` | 下载 → 自动删种 | 任务配置与既有操作 | P5 |
| `/userdownloader` | 设置 → 下载 → 自定义下载器 | 现有 REST 列表、连接测试、增删改 | P5 |
| `/mediafile` | 整理 → 文件管理 | 目录导航、识别转移、重命名、删除、字幕、硬链接查询 | P5 |
| `/unidentification` | 整理 → 手动识别 | 未识别列表、媒体匹配、季集/路径/转移参数 | P5 |
| `/history` | 整理 → 转移历史 | 筛选分页、重新识别、恢复、删除等已有动作 | P5 |
| `/tmdbcache` | 整理 → TMDB 缓存 | 查询、修改、删除、清空 | P5 |
| `/service` | 服务与工具 | 定时服务配置展示、手动触发、清理、名称识别、网络测试 | P5 |
| `/basic`、`/users` | 设置 → 通用/用户与权限 | 基础配置、用户管理、权限选择 | P5 |
| `/downloader`、`/download_setting`、`/indexer` | 设置 → 下载与搜索 | 下载器、下载参数、索引器设置及测试 | P5 |
| `/library`、`/mediaserver`、`/directorysync` | 设置 → 媒体与目录 | 媒体库、服务器、目录同步配置 | P5 |
| `/customwords`、`/filterrule` | 设置 → 识别与规则 | 识别词、规则组、默认规则、导入导出/恢复 | P5 |
| `/notification`、`/subtitle`、`/douban` | 设置 → 扩展服务 | 通知、字幕、豆瓣同步配置 | P5 |
| 未匹配路径 | 404 | 返回上一页或概览，避免循环重定向 | P2 |

全局组件另列回归清单：`AppHeader`、`AppSidebar`、`AppSearchbar`、`MessageCenter`、`RealTimeLogs`、`GlobalRequestProgress`、`GlobalOverlays`、`MediaCard`、`PersonCard`、`HelpTip`、`SeasonSelectDialog`、`AddRssMediaDialog`、`RssMediaDetailDialog`、`AddDownloadDialog`、`PageHeader`。未引用的占位组件先核对引用，再决定清理。

## 5. 接口、状态与兼容要求

### 5.1 后端契约

- 建立接口清单：调用页面、路径/命令、请求字段、返回结构、成功判据、权限、是否写入、是否支持分页/取消/批量。
- 保持 `/do` 的表单编码；保持 `/login_json`、`/logout_json`、`/upload`、`/api/v1/*` 等实际接口。不能在重构时统一改成 JSON POST。
- 特别验证 `MOV/TV` 与 `movie/tv`、字符串/数字 ID、分页参数、空值、日期、资源大小、目录分隔符、`code` 与 `retcode` 的差异。
- 下载删除当前 UI 提示会同时删除文件，`pt_remove` 封装仅传 ID：实施前确认真实行为；没有经验证的参数支持时，不增加“仅移除任务、保留文件”选项。
- 服务页当前部分状态由配置推导，展示为“已启用/未配置”；只有运行接口提供证据时才标“正在运行/已完成”。
- 新增预测进度、剩余时间、跨页全选、精确转移预览、后端取消等功能均先验证接口；没有能力时给出明确降级呈现。

### 5.2 会话与权限

- 使用 Cookie 会话和同源代理，正常登录设置会话；不在前端硬编码 Cookie，不把会话改存 localStorage。
- 建立单次并发共享的会话校验，区分未登录、无权限、网络失败和服务器异常；失败重试不能误放行或无限跳转。
- 明确刷新页面后如何恢复 `username/userpris`。优先使用经验证的现有会话能力；若后端没有可用的用户上下文接口，记录后端前置任务，禁止从 `get_users` 列表猜当前用户身份。
- 路由和动作按同一权限映射展示，普通用户不看到管理入口；实际授权仍由后端执行。
- 保留 `redirect`，仅允许应用内目标；退出或切换账号清理用户相关查询缓存、草稿和轮询。

### 5.3 请求与轮询

- 为查询、上传和长任务分别设置超时与反馈。长任务的网络等待不等于业务进度。
- 搜索与下载查询起始沿用 5 秒节奏，日志仅在打开时按原有需求刷新，消息按实际需求刷新；请求完成后再安排下一轮，避免慢请求重叠。
- 页面隐藏、路由离开、会话失效时暂停轮询，返回可见时刷新一次；失败使用退避，保留最后成功数据和更新时间，提供手动重试。
- 使用 AbortController/请求序号防止旧查询覆盖新结果；取消网络等待不宣传为取消后端任务。
- 搜索提交、下载添加、删除、保存、运行服务等写操作禁止自动重试；响应不明时提示核对结果，避免重复任务。
- 背景轮询不触发全局遮罩和重复 Toast。首次加载、静默更新、局部失败、写入中分别建模。

### 5.4 路由与状态恢复

- 保留旧路径及 query 语义，重点覆盖 `/search?q=&filters=&unident=`、`/media_detail?type=&id=`、人物详情、`/rss_history?t=`、`/mediafile?path=`。
- 清点“已有入口生成但页面未消费”的参数，不把参数存在等同于功能已实现；高级搜索条件需做端到端验证。
- 可分享的查询参数进入 URL；文件路径等保持既有语义但不额外传播敏感配置；密码、Cookie、令牌永不进入 URL。
- 列表返回恢复筛选、排序、分页和滚动锚点；详情/筛选全屏层有明确历史状态，浏览器返回先退出当前层。
- 媒体/任务存在稳定 ID 时用 ID；搜索任务目前按 keyword 操作，沿用真实契约，不捏造服务端 taskId。

## 6. 组件迁移策略

| 现有实现 | 目标 | 迁移注意 |
| --- | --- | --- |
| Element 容器、菜单 | QLayout、QHeader、QDrawer、QPageContainer、QPage；手机 QFooter + QTabs/QRouteTab | 导航来源统一；抽屉焦点、返回和滚动需要实测 |
| Element/VXE 表格 | QTable；手机 QTable grid 插槽或共享模型的 QCard/QList | 行标识、过滤、选择、分页、大数据性能等价 |
| Element Dialog/Drawer | 统一 AdaptiveDialog，内部 QDialog；筛选可用右侧面板 | 手机短操作底部面板，长表单全屏，避免多层模态堆叠 |
| Element 表单 | QForm、QInput、QSelect、QCheckbox、QToggle 等 | 校验触发、空值语义、标签和保存范围逐项核对 |
| ElMessage/ElMessageBox | 反馈服务封装 Notify/Dialog | 保留确认 Promise 语义；取消不充当业务选项 |
| loading/progress | QSkeleton、QInnerLoading、QLinearProgress、LoadingBar 等 | 网络传输进度与后台任务进度分别显示 |
| 图片/卡片/页头 | QImg、QCard 和业务组件 | 保留图片代理、失败占位、标题和状态可读性 |
| 图标/颜色/局部样式 | 单一图标集、语义 token、组件规范 | 移除 `--el-*`、深层 Element 选择器和硬编码尺寸 |

QTable 提供排序、分页、选择、grid 模式和虚拟滚动等能力；选用能力仍须结合本项目字段和数据规模验证。手机卡片化不能只开启 grid 就视为设计完成。[QTable 官方文档](https://quasar.dev/vue-components/table/)

页面首批迁移已完成，源码不再使用 Element Plus 组件或 VXE 表格；旧 UI 依赖已从 package.json 与锁文件清理，并通过完整构建确认依赖删除不会影响产物。

## 7. 分阶段执行任务

所有复选框表示未来任务。估算单位为人日，包含开发和模块自测，不是固定交付承诺。

### P0：运行基线与功能契约（3–5 人日）

- [x] 配置满足实际依赖 engine 的 Node 环境，执行 `npm ci`、`npm run type-check`、`npm run build`，记录已有问题。
- [ ] 在独立重构分支建立旧版可运行基线，留存可回滚的源码版本和构建产物。
- [ ] 对照第 4 节逐路由检查，记录入口、主要操作、权限、异常状态和依赖接口。
- [ ] 建立匿名化接口样例与动作契约，确认会话恢复、下载删除、分页、服务状态等不确定项。
- [ ] 在桌面 1440px、手机 390px 记录现有关键页面及流程，建立构建大小和性能基线。

出口：全量功能清单可追溯，现有缺陷与重构新增缺陷可区分，后端前置项有负责人和处理方式。

### P1：信息架构与设计基线（4–6 人日；依赖 P0）

- [ ] 依据 `style.md` 制作概览、搜索结果、详情、订阅、下载、文件管理、设置表单的桌面/手机稿，并补充平板行为。
- [ ] 确定导航归属、返回行为、主题 token、表单与危险操作规范。
- [ ] 补齐加载、空、错误、无权限、长文本、键盘弹出、批量模式及深色变体。
- [ ] 演练“搜索 → 下载”“发现 → 订阅”“下载暂停”“识别转移”“修改设置”五条主流程。

出口：设计可覆盖现有能力，核心流程无需依赖悬停、横向拖动宽表或猜测图标。

### P2：Quasar 基座与公共能力（5–8 人日；依赖 P1）

- [ ] 验证并锁定 Quasar/Vite/Sass/Node 组合，接入语言包、图标、浅深色主题。
- [ ] 建立统一布局、导航数据、断点策略、页头、路由元数据及登录/404 页。
- [ ] 统一会话与权限状态、API 错误、反馈服务、轮询和取消机制。
- [ ] 实现 AsyncState、AdaptiveDialog、FilterPanel、FormSection、ConfirmAction 等有真实复用需求的组件。
- [ ] 接入必要的 Vitest/Vue Test Utils、Playwright 和自动化检查；仅测试有行为价值的逻辑。

出口：390/768/1024/1440px 可完成登录、导航、主题切换；旧页面仍可工作；会话过期与普通用户场景通过。

### P3：核心垂直流程（6–9 人日；依赖 P2）

- [ ] 重构概览，采用模块独立加载与局部故障状态。
- [ ] 合并全局与页面高级搜索模型，迁移任务、结果、筛选与下载确认流程。
- [ ] 替换搜索 VXE 表格，验证至少 1,000 条模拟结果的筛选、滚动和选项可达性。
- [ ] 重构正在下载和新增下载，覆盖上传/磁力、开始、暂停、删除与刷新恢复。
- [ ] 验证操作失败、结果未知、任务切换和旧响应覆盖等边界。

出口：桌面与手机均可走通“搜索 → 筛选 → 下载 → 查看任务 → 暂停”，请求和状态逻辑共用。

### P4：探索与订阅（6–9 人日；依赖 P3）

- [ ] 迁移全部探索来源、榜单、每日放送、人物/媒体详情与最近下载。
- [ ] 统一媒体卡片、选季、添加/编辑订阅及订阅详情组件。
- [ ] 迁移电影/剧集订阅、历史、日历、自定义 RSS 和解析器。
- [ ] 验证订阅规则、站点、类型/季集参数、日历日期、分页与返回恢复。

出口：第 4 节 P4 路由及全局媒体弹窗逐项完成；手机日历能直接阅读日程并进入详情。

### P5：整理、服务与全部设置（8–12 人日；依赖 P4）

- [ ] 重构文件管理、手动识别、转移历史、TMDB 缓存；保留字幕和硬链接等低频能力。
- [ ] 重构服务工具、自动删种、自定义下载器和全部 13 个设置页面文件。
- [ ] 为复杂设置提供分组、基础/高级字段、局部校验、未保存提醒和明确保存范围。
- [ ] 为文件删除、移动、恢复、清空、规则导入等提供与接口真实行为匹配的确认和结果反馈。
- [ ] 核对不同账号的可见入口与动作、上传/导入/导出及连接测试。

出口：所有现有路径有可用 Quasar 实现，旧版功能清单无遗漏，普通用户流程不依赖管理权限。

### P6：完整验收与交付准备（4–6 人日；依赖 P5）

- [x] 清理 Element Plus、Element 图标、VXE、旧 CSS 与废弃组件；检查源码和锁文件。
- [ ] 执行全量路由冒烟、关键 E2E、视觉回归、无障碍、性能和真实手机检查。
- [ ] 验证生产代理、SPA history 回退、会话、图片和上传路径；更新 README 和部署示例。
- [ ] 形成验收记录、已知限制、版本说明及回滚说明，准备可部署产物。

出口：满足第 8 节。实际生产切换为后续部署工作；本次文档任务不执行部署。

**总估算：36–55 人日**，以熟悉 Vue 的开发者实施并获得设计/测试协作为前提。外部数据源异常、后端补充接口及既有严重缺陷另计；P0 结束后重新校准。关键依赖链为 P0 → P1 → P2 → P3 → P4 → P5 → P6。

## 8. 验收标准

### 8.1 功能与代码

- 全量旧路由、关键 query 和刷新深链接可用；P0 功能清单每项有结果记录。
- 桌面/手机同一动作调用同一业务服务，提交参数与原契约一致；切换断点不重复请求或丢状态。
- 已删除 UI 不再有依赖、注册、图标和样式残留；全部界面符合 `style.md`。
- `npm ci`、类型检查、生产构建及约定的测试命令通过，控制台无未处理异常。

### 8.2 行为测试

| 层级 | 必须覆盖 |
| --- | --- |
| 领域/单元 | 不同结果结构的成功失败判断、空值与枚举转换、排序/筛选、权限映射、轮询退避与取消、表单脏状态 |
| 组件 | 对话框焦点与返回、断点切换草稿保留、筛选应用/取消、行选择、提交防重复、错误定位 |
| E2E | 登录回跳/失效、搜索下载、电影及剧集订阅、下载控制、文件操作、设置保存、普通用户访问 |
| 异常 | 首次加载失败、部分成功、HTTP 200 业务失败、401/403、断网重连、慢响应、上传失败、图片失败 |

写入和删除类自动化在模拟接口或专用测试数据上执行；真实用户文件与下载任务不作为破坏性测试对象。

### 8.3 双端与视觉

- 基准视口：360×800、390×844、430×932、768×1024、1024×768、1440×900、1920×1080；320px 检查极窄布局兜底。
- 测试 599/600、1023/1024、1439/1440px 边界，横竖屏、软键盘、安全区、桌面 125% 缩放及 200% 文本缩放。
- 发布时记录并覆盖 Chrome、Edge、Firefox 当前与前一稳定版本；Safari 当前与前一主要版本；至少一台真实 iPhone 和 Android 手机，模拟器不能替代全部实机检查。
- 核心页面无整页横向滚动、主要操作被遮挡、无标签图标或依赖悬停才能使用的问题；深浅色均检查。
- 所有原型主流程可用键盘和触屏完成，焦点可见，主要触控区域至少 44×44 CSS px。

### 8.4 性能与恢复

- P0 固定数据集、机器、网络与测量方法，记录迁移前后结果；下列是目标而非当前测量结果。
- 在移动端模拟中端设备、4 倍 CPU 降速、固定 Fast 4G 条件下，使用可控接口测量关键页，3 次冷启动记录中位数：LCP ≤ 2.5s、CLS ≤ 0.1；交互延迟目标 ≤ 200ms，实验室结果不冒充线上 INP 分位值。
- 分开统计 API 耗时、图片耗时与前端渲染；内网/外部媒体源慢时仍有及时骨架、错误和恢复入口。
- 初始 JS gzip 相比 P0 基线不得增长超过 10%；若超出，分析后优先拆包、按需图标与路由懒加载，再重新验收。
- 搜索 1,000 条、下载 200 条、日志连续 10 分钟模拟场景可操作；日志有条数上限，轮询不重叠，离开页面后不持续增长定时器和内存。

## 9. 联调与回滚安排

用户提供的后端 `http://10.10.173.54:4000` 可在后续需要运行项目时作为联调目标。未来本地启动可使用进程级配置，不必修改受版本管理的环境文件：

```powershell
$env:FLASK_TARGET = 'http://10.10.173.54:4000'
npm run dev
```

当前配置默认监听 5174。`VITE_FLASK_BASE` 维持空值，通过开发服务器同源代理访问接口。`FLASK_TARGET` 当前由 `process.env` 读取，不应假设写入普通 `.env` 后一定会被该配置自动加载。保留 `/do` 精确路径处理，避免误代理 `/downloading` 等 SPA 路由。

本次验收已执行 Vite 开发服务器路由可达性冒烟：根路径、登录页、40 个业务路由和 404 回退共 42 个地址全部返回 HTTP 200，`/src/main.ts` 入口资源返回 HTTP 200。该检查不替代浏览器渲染、真机触控和关键业务流程验收。

已提供的会话凭据仅在必要的临时联调会话使用，不抄入文档、源码、环境文件或测试样例。浏览器是否能使用会话取决于请求域和 Cookie 属性；先验证正常登录与同源代理，不能认为设置服务器地址就已完成登录。

每阶段保留可构建版本；阶段回滚通过受控还原对应提交或部署上一版本产物完成。上线前保留上一前端产物与代理配置；本轮不改变后端数据结构，避免引入前端回退还需数据库回退的依赖。

## 10. 主要风险与处理

| 风险 | 处理与验收门槛 |
| --- | --- |
| 两套 UI 暂时共存导致 CSS 冲突、包体变大 | 限定过渡期，逐模块回归；最终移除旧库并复测包体 |
| 搜索大表卡片化后性能下降 | P3 提前验证分页/虚拟滚动；按容器空间选表现，保留共享数据模型 |
| 删除、恢复、移动语义不清 | P0 核对请求与后端行为；只展示可验证的选项和结果 |
| 登录探测或权限不完整 | 优先解决会话恢复；没有接口能力时记录后端依赖，不用 UI 隐藏替代授权 |
| 旧字段、设置或低频页面遗漏 | 路由、页面、全局组件、接口动作四份清单交叉验收 |
| 轮询丢失状态或后台耗电 | 单请求在途、可见性管理、退避、更新时间及手动恢复 |
| 断点切换或系统返回丢草稿 | 状态置于稳定父层；原型和组件测试覆盖退出/返回/旋转 |
| 原生打包需求后续加入 | 作为范围变更单独估算 CLI、平台能力、签名和分发，不挤入本轮 SPA 验收 |

本计划的功能边界以源码盘点为基础；视觉与交互细节以 [style.md](./style.md) 为准。实施过程中发现冲突时同步更新两份文档及验收条目。
