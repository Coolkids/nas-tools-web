# NAStool Web 逐页重构说明

> 日期：2026-09-13。状态：实施中。本文依据当前源码细化每个页面的修改方式，PG01–PG40 已完成首批迁移，正在进行真实接口联调、浏览器验收和体验收口。总体阶段见 [task.md](./task.md)，视觉与通用交互规范见 [style.md](./style.md)。

## 1. 使用方式与覆盖范围

本文覆盖 `src/views` 下全部 **40 个页面文件**。`RankingView.vue` 同时服务 `/ranking` 和 `/bangumi`；加上 `/` 重定向，共覆盖当前 **42 条路由配置**，包括 404。页内对话框、全局弹层和导航外壳另有修改清单，不能只替换页面主体而保留旧弹窗。

每个页面按“现状与改造目标 → 桌面布局 → 手机布局 → 操作与数据 → 组件/逻辑 → 验收”执行。以下组件名如 `SearchWorkspace`、`DirectoryPicker` 为建议新建或提取的项目组件，不表示仓库已存在；`Q*` 为目标 Quasar 组件。接口名和字段来自源码，真实返回语义仍需按 task.md 的 P0 阶段联调确认。

### 1.1 全页面共同要求

- 保留已有路径、查询参数、权限和业务能力。手机功能入口可分层，但不得直接删除低频功能。
- 外壳遵守 style.md：手机 `<600px`；平板 `600–1023px`；桌面 `≥1024px`，宽桌面 `≥1440px`。容器过窄时内部仍可使用卡片。
- 每页保留一个业务模型；两端共用查询、选择集、表单草稿、校验和提交函数。切换宽度不重复请求、不丢草稿。
- 移除页面重复 padding、固定大弹窗宽度、`--el-*` 与旧组件深层选择器；字号、颜色、尺寸从 style.md 的 token 获取。
- 每页都要有首次加载、空数据、筛选无结果、失败重试、无权限和提交中状态；后台刷新保留有效内容。详情主数据不存在时禁用依赖它的动作。
- 查询条件和浏览位置能在返回时恢复；敏感字段不进 URL/持久化缓存。写请求防重复、不自动重试；业务失败不能显示成功。
- 桌面表格统一 QTable；手机用共享数据的 QCard/QList 或 grid 插槽。所有主要触控区域至少 44×44px。
- 长表单手机全屏，底部保存栏替换底部导航；只读详情操作栏若与导航同时存在，正文必须预留高度。
- 每页验收同时执行公共验收：390px、768px、1440px，浅深色、键盘操作、真实触屏、长文本、慢请求及返回恢复。

### 1.2 页面索引

| 编号 | 页面 | 旧路由 | 阶段 |
| --- | --- | --- | --- |
| PG01 | 登录 | `/login` | P2 |
| PG02 | 概览 | `/` → `/index` | P3 |
| PG03 | 资源搜索 | `/search` | P3 |
| PG04 | 服务与工具 | `/service` | P5 |
| PG05 | 404 | `/:pathMatch(.*)*` | P2 |
| PG06 | 推荐 | `/recommend` | P4 |
| PG07 | 榜单/每日放送 | `/ranking`、`/bangumi` | P4 |
| PG08 | 豆瓣电影 | `/douban_movie` | P4 |
| PG09 | 豆瓣剧集 | `/douban_tv` | P4 |
| PG10 | TMDB 电影 | `/tmdb_movie` | P4 |
| PG11 | TMDB 剧集 | `/tmdb_tv` | P4 |
| PG12 | 媒体详情 | `/media_detail` | P4 |
| PG13 | 人物作品 | `/discovery_person` | P4 |
| PG14 | 最近下载 | `/downloaded` | P4 |
| PG15 | 电影订阅 | `/movie_rss` | P4 |
| PG16 | 电视剧订阅 | `/tv_rss` | P4 |
| PG17 | 订阅历史 | `/rss_history` | P4 |
| PG18 | 订阅日历 | `/rss_calendar` | P4 |
| PG19 | 自定义订阅 | `/user_rss` | P4 |
| PG20 | RSS 解析器 | `/rss_parser` | P4 |
| PG21 | 正在下载 | `/downloading` | P3 |
| PG22 | 自动删种 | `/torrent_remove` | P5 |
| PG23 | 自定义下载器 | `/userdownloader` | P5 |
| PG24 | 文件管理 | `/mediafile` | P5 |
| PG25 | 手动识别 | `/unidentification` | P5 |
| PG26 | 转移历史 | `/history` | P5 |
| PG27 | TMDB 缓存 | `/tmdbcache` | P5 |
| PG28 | 基础设置 | `/basic` | P5 |
| PG29 | 自定义识别词 | `/customwords` | P5 |
| PG30 | 目录同步 | `/directorysync` | P5 |
| PG31 | 豆瓣同步 | `/douban` | P5 |
| PG32 | 下载器 | `/downloader` | P5 |
| PG33 | 下载设置 | `/download_setting` | P5 |
| PG34 | 索引器 | `/indexer` | P5 |
| PG35 | 媒体库 | `/library` | P5 |
| PG36 | 媒体服务器 | `/mediaserver` | P5 |
| PG37 | 消息通知 | `/notification` | P5 |
| PG38 | 字幕 | `/subtitle` | P5 |
| PG39 | 用户管理 | `/users` | P5 |
| PG40 | 过滤规则 | `/filterrule` | P5 |

## 2. 应用外壳先行修改

| 源文件 | 具体修改 | 必须保留/验证 |
| --- | --- | --- |
| `src/App.vue`、`src/layouts/DefaultLayout.vue` | 用 QLayout/QHeader/QDrawer/QPageContainer/QPage 组织统一外壳；公开登录页独立布局；手机增加底部导航 | route.meta.public 语义、RouterView、滚动恢复、全局弹层挂载位置 |
| `src/components/AppSidebar.vue` | 提取导航数据，按概览/探索/订阅/下载/整理/服务/设置组织；支持迷你/常驻/覆盖三种展示 | 全部旧路由可达，权限过滤一致，当前分组展开；抽屉键盘焦点管理 |
| `src/components/AppHeader.vue` | 品牌、全局搜索、消息、账号；手机缩减为当前标题及必要入口 | 版本信息、退出登录等已有能力；主题入口为本轮新增偏好能力 |
| `src/components/AppSearchbar.vue` | 保留搜索跳转；高级搜索表单提取到与 PG03 共用的模型/组件 | `q/filters/unident` 等参数实际消费，不再维护两份条件拼接逻辑 |
| `src/components/PageHeader.vue` | 统一标题/说明/计数/操作插槽；手机主操作常显、次操作收纳 | 页标题不与顶栏无意义重复；刷新、帮助可达 |
| `src/router/index.ts` | 保留路由名与路径，补充分组元数据、返回来源、会话校验和滚动恢复 | 深链接刷新、无权限、并发导航、404、登录回跳 |
| `src/styles/index.scss`、`src/main.ts` | 后续按 P2 接入 Quasar 主题/语言/图标，收敛尺寸；P6 才清掉全部旧依赖 | 不在本次文档工作中修改；迁移中旧页仍可工作 |

“更多”面板和设置分组导航可作为外壳组件实现，不要求增加新的业务页面或改旧 URL。新增的结果/编辑视图优先在现有路由下管理 UI 历史；确需增加 query 时与旧参数兼容，并定义直接打开时的回退。

## 3. 顶层页面

### PG01 登录

**源文件：** `src/views/LoginView.vue`。**路由：** `/login`。

- **现状与目标：** 将现有 Element 登录表单与局部响应式样式迁至统一主题，保留已有背景来源逻辑；背景失败时表单仍可独立显示。
- **桌面：** 简洁品牌区 + 约 400px 登录表单；背景弱化，不新增大面积营销内容。表单依次为用户名、密码、记住登录、错误信息、登录按钮。
- **手机：** 单列表单，左右 24px；输入区域 48px 高。纵向内容可滚动，避免横屏和软键盘下固定居中导致按钮被裁切。
- **操作与数据：** 保留 `login()`、remember 和 `redirect`。密码可显示/隐藏；Enter 提交；一次在途提交；登录失败保留用户名，错误置于表单附近。只接受站内回跳目标，外部地址回退概览。
- **组件/逻辑：** QForm/QInput/QCheckbox/QBtn/QBanner；提取会话提交逻辑到共享认证能力，用户名与权限初始化不分散到页面。
- **验收：** 正确/错误密码、记住登录、网络失败、带 query 的回跳、键盘遮挡、背景图失败均通过；表单不记录用户提供的临时会话凭据。

### PG02 概览

**源文件：** `src/views/HomeView.vue`。**路由：** `/index`，`/` 继续重定向。

- **现状与目标：** 将媒体数量、磁盘、转移趋势和播放历史从整页 loading 拆成独立区域；失败数量不显示成 0。
- **桌面：** 页头“概览 + 刷新”；异常横幅；四项媒体摘要；下方空间/趋势按 4:8 或 8:4 排布，播放历史放下一行。图表颜色使用主题 token。
- **手机：** 异常信息 → 两列数量摘要 → 空间 → 最近活动；趋势图放后面并可展开。图表提供文本摘要，历史由宽表变为“时间 + 事件”列表。
- **操作与数据：** 保留 `getLibraryMediacount/getLibrarySpacesize/getTransferStatistics/getLibraryPlayhistory`；每块独立错误与重试。全局刷新并发加载，保留刷新前内容。无法确认未配置还是连接故障时使用准确的通用失败说明。
- **组件/逻辑：** 提取 `MetricCard`、`StorageSummary`、`TransferTrend`、`PlayHistoryList` 和 `useOverview`；可保留现有 SVG 趋势实现并修正可访问性，无需为重构强加图表库。
- **验收：** 任意一个接口失败不遮住其他数据；空值、百分比字符串、图表全零/单点/不同日期范围正确；手机无图表横向溢出。

### PG03 资源搜索

**源文件：** `src/views/SearchView.vue`。**路由：** `/search`。

- **现状与目标：** 拆开搜索输入、任务列表、结果、下载和订阅表单；去掉依赖 DOM 测高的巨大 VXE 结果弹窗，合并重复高级搜索逻辑。
- **桌面：** 页头搜索输入 + 高级搜索；宽屏左侧约 280px 搜索任务，右侧当前任务标题/状态/筛选/结果表。1024px 或空间不足时任务列表进入结果视图，避免主从区都不可读。
- **手机：** 搜索输入 → 任务摘要列表；点击任务进入全屏结果视图。每条资源显示最多三行名称、站点、大小、做种、已有促销数据，保留下载和更多入口。高级筛选单独面板，“应用/取消/重置”语义明确。
- **操作与数据：** 保留普通/高级搜索、任务删除、结果筛选、站点跳转、下载、添加订阅。高级条件保留类型、名称、年份、季、质量、分辨率、促销、规则；消费旧 `q/filters/unident`。明确搜索提交由谁触发，详情已提交搜索后进入此页不得再提交一次。
- **状态处理：** 任务按 keyword 的现有接口契约选择；切换任务立即清除旧结果归属，旧响应不能覆盖新任务。排队/运行/完成/失败分别展示；正在运行不提示“无结果”。保留任务列表的 5 秒查询需求，转为非重叠、可暂停、可退避的轮询。
- **弹层：** 下载复用共享下载表单，订阅类型用显式“电影/电视剧”单选，取消只关闭。结果返回任务列表保留位置，关闭下载表单保留资源筛选。
- **组件/逻辑：** `SearchForm`、`SearchTaskList`、`SearchResults`、`TorrentResultCard`、`useSearchTasks`；结果使用 QTable/QList/QChip，查询模型共用。大结果集用分页/虚拟滚动，取消 `recalcTable` 对旧 CSS 结构的依赖。
- **验收：** 旧链接带条件生效；A/B 任务快速切换无串数据；1,000 条模拟结果可筛选和下载；删除搜索记录不误删下载；后台轮询失败可恢复。

### PG04 服务与工具

**源文件：** `src/views/ServiceView.vue`。**路由：** `/service`。

- **现状与目标：** 将服务表、清理动作、名称识别和网络测试明确分区；配置启用与实时运行状态分开。
- **桌面：** “定时服务 / 手动工具”导航，服务表列为名称、配置状态、周期、运行；清理放独立危险区域。名称识别结果分原始名称、识别媒体、季集、格式标签、替换过程等已有内容。
- **手机：** 服务摘要卡片显示状态和周期，“运行”单个入口；工具全屏。识别结果纵向分组，网络测试按“对象/连通性/耗时”列表展示。
- **操作与数据：** 保留配置推导服务、`runScheduler`、两类清理、`nameTest/netTest`、复制与 TMDB/外部链接。运行前确认对象；受理和完成依据实际接口区分，不能从启用配置推断正在运行。
- **组件/逻辑：** `ServiceList`、`NameTestPanel`、`NetworkTestPanel`，QTable/QCard/QBadge；识别结果呈现与文件管理复用，工具自己的 pending 不阻塞整页。
- **验收：** 未启用与执行失败可区分；清理黑名单/RSS 历史影响明确；部分网络目标失败不抹除其余结果；名称识别复制在手机可用并有失败回退。

### PG05 404

**源文件：** `src/views/NotFoundView.vue`。**路由：** `/:pathMatch(.*)*`。

- **现状与目标：** 将 Element 结果组件换成轻量 QIcon/QBtn 内容区，保留“页面不存在”说明。
- **桌面：** 在正常外壳内容区显示标题、说明、“返回上一页”“前往概览”；不用大插图占满屏幕。
- **手机：** 同样操作纵向排列，底部导航可正常使用；无有效站内历史时主按钮为概览。
- **操作与数据：** 不请求业务数据。未知路径按现有认证策略处理；不得返回登录/404 循环；不要把无权限页面伪装成 404。
- **验收：** 地址栏直接打开未知 URL、刷新、登录后返回未知路径和无历史记录场景均能退出。

## 4. 探索与详情页面

### 探索列表的共享改造

PG06、PG08–PG11、PG14 共用 `MediaBrowsePage` 展示模板，保留各 route view 作为轻量入口；是否合并文件由后续实现决定，URL 不变。改造 `src/composables/useDiscovery.ts`：首次按可视区域加载，不默认无条件连续拉三页；分页失败保留重试，不设为“没有更多”；使用实际滚动容器或观察器，卸载统一清理；筛选切换取消/忽略旧响应；按来源 + 媒体类型 + ID 去重。列表收藏/订阅状态更新按稳定身份定位，不依赖数组索引。

### PG06 推荐

**源文件：** `src/views/discovery/RecommendView.vue`。**路由：** `/recommend`。

- **现状与目标：** 保留趋势/分类及 query 驱动的推荐用途，去掉每页复制的大排来源按钮；补齐 `source` 等真正影响数据的监听与查询键。
- **桌面：** 来源导航 + 当前推荐标题/说明 + 海报网格；榜单“查看全部”进入时保留对应分类标题。
- **手机：** 来源入口收纳为选择器，当前来源清晰；两列海报，列表末尾加载更多/重试，不用无限自动请求掩盖错误。
- **操作与数据：** 保留 `type/subtype/title/subtitle/week/tmdbid/personid/keyword/source` 的上下文；卡片进入 PG12，订阅复用媒体动作。
- **组件/逻辑：** `MediaBrowsePage`、`DiscoveryNavigation`、`MediaCard`，QImg/QCard/QSkeleton，复用改造后的 `useDiscovery`。
- **验收：** 从榜单进入完整列表参数不丢；快速切来源无重复/混合结果；返回恢复已加载页和滚动位置。

### PG07 榜单与 Bangumi 每日放送

**源文件：** `src/views/discovery/RankingView.vue`。**路由：** `/ranking`、`/bangumi`。

- **现状与目标：** 保留同一页面按 route.name 选择榜单/每日放送的实现；分类独立加载，避免所有榜单失败呈现同一种空白。
- **桌面：** 榜单按来源分行，标题右侧“查看全部”、横向列表两端显式翻动按钮；Bangumi 提供星期一至日切换与当日网格。
- **手机：** 榜单默认展示有限条目并提供完整列表入口；每日放送以当前星期为明确状态，可点选其他日期。滑动不是唯一查看途径。
- **操作与数据：** 保留现有 TMDB、豆瓣各榜单的 type/subtype，Bangumi 的 week 值；标题、来源、请求参数一一对应。视口临近或选中分类才按需加载。
- **组件/逻辑：** `RankingSection`、`WeekSelector`、共享 MediaCard，分类级 AsyncState。
- **验收：** 两个旧路由有正确标题/选中状态；七天内容正确对应；单榜失败不影响其他榜；“查看全部”不变成默认推荐。

### PG08 豆瓣电影

**源文件：** `src/views/discovery/DoubanMovieView.vue`。**路由：** `/douban_movie`。

- **现状与目标：** 使用共享浏览模板，保留电影专属“排序 + 类型”筛选。
- **桌面：** 页头标明豆瓣电影，筛选行保留默认/综合/首播/高分/近期热度及电影类型，下面海报网格。
- **手机：** 常显当前排序，类型进入筛选面板；应用后显示筛选 chip。两列海报，详情返回不丢电影条件。
- **操作与数据：** 固定 `type=DOUBANTAG`、`subtype=MOV`；原 sort 值及 tags 中文值保持；保留豆瓣图片代理。
- **组件/逻辑：** MediaBrowsePage + FilterPanel；补齐共享滚动清理，移除该页独立媒体查询和重复导航。
- **验收：** 每个排序对应原参数，清除类型能回全部，图片失败不移位，分页重试不会重复追加上一页。

### PG09 豆瓣剧集

**源文件：** `src/views/discovery/DoubanTvView.vue`。**路由：** `/douban_tv`。

- **现状与目标：** 共用浏览模板，保留“排序 + 地区”，不套用电影类型选项。
- **桌面：** 地区选项含现有华语、中国大陆/香港/台湾、欧美、韩国、日本等，选中摘要与网格并列于明确层级。
- **手机：** 地区选择在底部筛选面板，排序保留快捷入口；卡片展示剧集类型，订阅进入选季流程。
- **操作与数据：** `DOUBANTAG/TV`；地区仍按现有 tags 字段传输；来源/类型切换不共享错误的 filter 值。
- **组件/逻辑：** MediaBrowsePage、FilterPanel、共享订阅表单；分页和回退按探索共享要求执行。
- **验收：** 同名电影/剧集不混淆，选择地区有效；从作品返回地区条件和列表位置正确。

### PG10 TMDB 电影

**源文件：** `src/views/discovery/TmdbMovieView.vue`。**路由：** `/tmdb_movie`。

- **现状与目标：** 保留电影类别和原始语言筛选，转为一致的浏览页面。
- **桌面：** 类型、语言常显筛选，网格突出标题/年份/评分；来源标记明确。
- **手机：** 类型与语言汇入一个筛选面板，可分别清空，已应用条件显示 chip。
- **操作与数据：** `DISCOVER/MOV`；`with_genres` 使用现有电影数字 ID，`with_original_language` 保留语言代码。未提供的年份/评分范围不擅自加成“可用筛选”。
- **组件/逻辑：** MediaBrowsePage，电影筛选配置独立；共用图片、订阅和查询状态。
- **验收：** 类型 ID 不变成标签字符串；语言空值正确；筛选、翻页、返回组合可恢复。

### PG11 TMDB 剧集

**源文件：** `src/views/discovery/TmdbTvView.vue`。**路由：** `/tmdb_tv`。

- **现状与目标：** 保留剧集独立类别字典，禁止复用电影 ID 集合导致查询错误。
- **桌面：** 与 TMDB 电影一致的布局，类别使用动作冒险、纪录、儿童、真人秀等现有剧集分类。
- **手机：** 相同筛选容器，卡片按剧集处理，订阅进入季集选择；无季信息时显示可重试状态。
- **操作与数据：** `DISCOVER/TV`；保留剧集 `with_genres` 与语言值。与 PG10 可共用组件但不共用类型专属配置。
- **组件/逻辑：** MediaBrowsePage + TV filter config + 共享订阅组件。
- **验收：** 两个 TMDB 路由切换无筛选泄漏，剧集分类参数正确，选季后订阅类型为 TV。

### PG12 媒体详情

**源文件：** `src/views/discovery/MediaDetailView.vue`。**路由：** `/media_detail?type=&id=`。

- **现状与目标：** 保留背景、海报、简介、演职员、推荐及媒体操作；修正类型判断分散与关联请求缺少独立错误处理的问题。
- **桌面：** 海报与标题/元信息/主动作并排，下方简介、演职员和相关推荐；装饰背景限定高度，文字放可读层面。
- **手机：** 返回 → 标题/年份/类型/评分 → 订阅与搜索 → 简介 → 季集/演员/推荐。海报缩小，主动作可吸底并留安全区。
- **操作与数据：** `mediaDetail` 成功后加载 `mediaRecommendations/mediaPerson`；各自错误可重试且防旧响应覆盖。统一 `TV/tv`、`MOV/movie` 映射，避免小写 tv 被视为电影。保留搜索和取消订阅能力；已订阅主按钮改为查看/管理，移除订阅放明确次动作并确认。
- **组件/逻辑：** `MediaHero`、`MediaActionBar`、`PeopleSection`、`RelatedMediaSection`；共用订阅表单与选季，不让收藏标记变化冒充服务器已成功。
- **验收：** 无 ID/无结果有返回入口；关联接口失败不使详情消失；电影与各大小写剧集类型正确；搜索不会在跳转后重复提交。

### PG13 人物作品

**源文件：** `src/views/discovery/PersonView.vue`。**路由：** `/discovery_person`。

- **现状与目标：** 保留人物作品分页及电影/剧集切换，替换依赖 `.app-main` 的滚动监听。
- **桌面：** 人物标题/已有头像信息 + 电影/剧集 Tab + 作品网格；没有传记等数据时不新增空栏目。
- **手机：** 紧凑人物摘要、双选 Tab、两列作品；返回媒体详情保留原详情位置。
- **操作与数据：** 保留人物 ID 的 `id/tmdbid` 兼容及 `name/title/type` 参数；切换作品类型重置对应页，不把旧请求追加到新类型。
- **组件/逻辑：** `PersonHeader` + MediaGrid，`usePersonWorks` 管理按人物/类型的查询；统一分页重试和卸载清理。
- **验收：** 两种 ID 参数均可打开；无姓名有可靠默认标题；少量作品/多页作品、类型快速切换正确。

### PG14 最近下载

**源文件：** `src/views/discovery/DownloadedView.vue`。**路由：** `/downloaded`。

- **现状与目标：** 保留媒体式浏览，入口归入“下载”；不能把最近下载媒体列表当作下载器实时任务列表。
- **桌面：** 下载模块子导航 + 最近下载海报网格；显示来源返回的日期等已有字段，日期不明时不强行按天分组。
- **手机：** 默认小海报摘要列表，突出名称、类型、年份和实际下载日期；查看详情为主要操作。
- **操作与数据：** 保留 `type=DOWNLOADED` 的 getRecommend 分页；媒体详情和已有订阅操作复用。刷新保留旧数据，结束和失败分别呈现。
- **组件/逻辑：** MediaBrowsePage 的下载变体，可切换 MediaCard/MediaSummaryRow，但数据只加载一份。
- **验收：** 与“正在下载”互相可导航，详情返回原位置，不显示没有依据的进度或“入库成功”。

## 5. 订阅页面

### PG15 电影订阅

**源文件：** `src/views/rss/MovieRssView.vue`。**路由：** `/movie_rss`。

- **现状与目标：** 保留订阅卡片和详情/编辑联动，将电影与剧集重复加载、规则名称解析和动作逻辑提取为共享订阅列表能力。
- **桌面：** 电影/剧集切换 + 日历/历史/自定义入口；页头“添加电影订阅”；默认海报摘要卡，可提供管理列表模式。摘要保留名称、年份、状态、制作组、规则、站点，低频信息折叠。
- **手机：** 小海报 + 标题/年份/状态 + 一行规则摘要，点开订阅详情；常显添加入口，状态不覆盖海报主体。列表局部筛选可新增为前端能力，不额外创建服务端任务。
- **操作与数据：** `getMovieRssList`、规则名称映射、添加/编辑、详情删除/搜索/刷新和历史跳转全部保留；保存成功刷新对应项。读取详情失败不可拿空对象覆盖已有设置。
- **组件/逻辑：** `SubscriptionList`、`SubscriptionCard`、`useSubscriptions('MOV')`；详情与表单按第 9 节统一重构，电影表单不显示季集字段。
- **验收：** 添加、编辑、刷新、搜索、移除一条电影订阅完整通过；规则被删除或站点缺失时显示原值/失效提示，不擅自换成默认值。

### PG16 电视剧订阅

**源文件：** `src/views/rss/TvRssView.vue`。**路由：** `/tv_rss`。

- **现状与目标：** 使用订阅共享列表，但保留剧集季、总集数、已完成/剩余集数的独立语义。
- **桌面：** 小海报、剧名/季、进度、状态、规则/站点摘要；同一剧不同季分别可辨认，管理模式便于对比进度。
- **手机：** 名称和季在首行区域，进度条下方有“已完成/总数”文字；总集数未知时不用 0% 假进度，显示已知信息和未知说明。
- **操作与数据：** `getTvRssList` 和 `TV` 参数不变；编辑保留季、开始订阅集数、总集数等字段。单季/多季选择按现有接口调用方式处理，批量时逐项核对结果。
- **组件/逻辑：** `useSubscriptions('TV')`、`EpisodeProgress`、共享 SubscriptionForm；不再复制 stateMeta/progressOf 等函数到多个页面和弹窗。
- **验收：** 同名多季不互相覆盖；未知总集数、零集、完成订阅展示准确；手机编辑季集后旋转屏幕不丢输入。

### PG17 订阅历史

**源文件：** `src/views/rss/RssHistoryView.vue`。**路由：** `/rss_history?t=`。

- **现状与目标：** 将海报/标题/长简介/时间/操作宽表改为摘要优先的历史模板，保留类型与分页。
- **桌面：** 电影/剧集类型切换，表格列为媒体、季集摘要、完成时间、操作；简介默认两行，可展开。
- **手机：** 按条展示海报、标题、类型/季集、时间；主动作“重新订阅”，删除记录放更多；顶部返回电影或剧集订阅来源。
- **操作与数据：** 保留 `getRssHistory`、删除、重新订阅表单及 `t` 参数；重新订阅先回填媒体/季集，不把“历史有此记录”当成当前已订阅。
- **组件/逻辑：** `SubscriptionHistoryList` + QTable/QList + 共享 SubscriptionForm，分页与类型的状态用同一查询模型。
- **验收：** 类型切换回第一页；末页删除后能回有效页；重新订阅失败不移除历史；记录删除文案不暗示删除媒体文件。

### PG18 订阅日历

**源文件：** `src/views/rss/RssCalendarView.vue`。**路由：** `/rss_calendar`。

- **现状与目标：** 保留周/月/日程三种现有视图；调整默认桌面月历、手机日程，不删除周视图。优化按每个订阅请求日历导致的并发压力。
- **桌面：** 页头月份/日期范围、上一段/今天/下一段、视图切换；月格只放少量事件，溢出显示“另有 N 项”进入当日清单。
- **手机：** 日期条 + 当日日程，事件显示小海报、名称、季集/日期等实际字段；可切换周/月，月格不硬塞完整海报。无事件日期有清晰空态。
- **操作与数据：** 保留电影/剧集订阅列表及 `movie_calendar_data/tv_calendar_data`，限制并发（初始建议 4 个）、缓存同一媒体/季查询。现有接口按订阅请求，不假定支持日期范围分页；月份切换优先筛已有数据，刷新才更新。部分失败标明“部分日程未加载”。
- **组件/逻辑：** `CalendarToolbar`、`MonthGrid`、`WeekGrid`、`AgendaList`、`useSubscriptionCalendar`；网格可用 CSS，Quasar 负责控件，不引入未经选型的日历扩展作为必需项。
- **验收：** 周/月/日程切换保留选中日期；跨月/跨年/不同季正确；日期字符串不被时区转换错移一天；失败项重试不重复事件。

### PG19 自定义订阅

**源文件：** `src/views/rss/UserRssView.vue`。**路由：** `/user_rss`。

- **现状与目标：** 拆分任务列表、任务编辑、RSS 条目、下载历史和条目测试，保留下载与订阅两种 uses 模式及全部规则设置。
- **桌面：** 任务表显示名称、状态、用途、刷新间隔、地址摘要；“查看条目”主入口，运行/编辑/历史/删除分级。详情展开基础设置和规则摘要，敏感 RSS 地址查询串不在列表铺开。
- **手机：** 任务卡显示名称、用途、状态和周期；操作菜单进入条目/历史。编辑全屏分为“来源与解析 → 匹配规则 → 下载或订阅设置”，顶部可返回，底部保存。
- **字段安排：** 来源组包含名称、状态、interval、address、parser、uses；匹配组包含 include/exclude、识别要求、rule、restype/pix/team、over_edition；输出组按模式展示 save_path/download_setting 或 rss_sites/search_sites。原条件组合与空值意义保持。
- **操作与数据：** 保留 `getUserRssTasks/getUserRssTaskDetail/updateUserRssTask/deleteUserRssTask/runUserRss`；条目保留列表、单条/批量下载、批量检查、测试；历史保留查看。选项来源用已有解析器、站点、索引器、规则和下载设置接口。
- **子视图：** 条目桌面表格、手机标题/发布时间列表；显式选择模式显示范围和数量，运行/下载防重复。测试结果在当前子视图展开，避免三层弹窗；切换任务清除上个任务的选择集。
- **组件/逻辑：** `UserRssTaskList`、`UserRssForm`、`RssArticleList`、`RssArticleTest`、`RssDownloadHistory`，独立请求状态，共享父层当前任务 ID。
- **验收：** 两种用途新增/编辑字段完整；缺失解析器有处理入口；批量结果逐项汇总；退出编辑回条目或列表的正确位置；“运行完成”只在有依据时显示。

### PG20 RSS 解析器

**源文件：** `src/views/rss/RssParserView.vue`。**路由：** `/rss_parser`。

- **现状与目标：** 保留解析器 CRUD，改善 XML/JSON 格式与附加参数编辑；此页当前没有独立解析测试动作，不把条目测试虚构成现有能力。
- **桌面：** 表格显示名称、XML/JSON 类型、附加参数摘要；编辑 720px 左右，格式与参数为多行区，提供语法用途说明。
- **手机：** 摘要列表 + 全屏编辑，字段顺序名称、类型、格式、附加参数；文本可换行、滚动与复制。
- **操作与数据：** 使用现有 `getRssParserList/getRssParserDetail/updateRssParser/deleteRssParser`；格式字段可能是解析表达式，不能简单当 XML/JSON 正文校验而拦截合法格式。删除前说明自定义订阅可能引用它，具体引用/阻止删除仅在有数据依据时提供。
- **组件/逻辑：** `RssParserForm` + QForm/QInput(type=textarea)/QSelect；可引导返回 PG19 用现有条目测试，新增独立测试需要接口确认。
- **验收：** XML/JSON 类型及长表达式原样往返；取消编辑不写入；详情加载失败不允许保存空表单；从自定义订阅进入后可返回原任务草稿。

## 6. 下载页面

### PG21 正在下载

**源文件：** `src/views/download/DownloadingView.vue`。**路由：** `/downloading`。

- **现状与目标：** 将宽任务表和统一页面状态改为响应式任务列表及按任务的动作状态；修正“非 Downloading 一律暂停”的显示规则。
- **桌面：** 顶部新增下载/刷新；任务表保留标题/原名、海报、速度、进度、状态、开始/暂停/更多。状态筛选可按已加载真实状态本地完成。
- **手机：** 一任务一卡片，标题最多两行可展开，状态文字、进度条、速度紧凑排列；开始/暂停常显，删除放更多。
- **操作与数据：** 保留 `getDownloading/ptStart/ptStop/ptRemove`，核对 retcode；5 秒刷新非重叠、隐藏暂停、网络失败退避并可恢复。更新按任务 ID 合并，列表滚动和选择不跳动。
- **新增/删除：** 新增下载共享文件/磁力表单。删除请求当前仅传 ID，按经验证的实际删文件行为确认，不新增无后端支持的“保留文件”开关；这与 PG22 的已有动作枚举分开处理。
- **组件/逻辑：** `DownloadTaskTable`、`DownloadTaskCard`、`useDownloadTasks`、QLinearProgress；进度规范化 0–100，未知状态保留可读原值。
- **验收：** 下载中/排队/做种/暂停/未知状态不混淆；业务失败不误报控制成功；首次加载不闪空；新增后查看任务、失联恢复、后台暂停轮询均通过。

### PG22 自动删种

**源文件：** `src/views/download/TorrentRemoveView.vue`。**路由：** `/torrent_remove`。

- **现状与目标：** 保留任务 CRUD、手动执行和预处理种子列表；将长表单分组，突出动作风险及筛选条件。
- **桌面：** 列为任务名、启用状态、下载器、动作、周期、条件摘要、操作；“预览匹配种子”优先展示，“运行”与“删除配置”分开。
- **手机：** 卡片首屏明确动作是暂停/删除种子/删除种子及文件；编辑全屏分为“任务与调度”“适用范围”“触发条件”“核对”。预览为资源名称/站点/大小列表。
- **字段安排：** 保留 enabled、interval、samedata、onlynastool、downloader；条件包括 ratio、seeding_time、upload_avs、size、tags、savepath_key、tracker_key，Qb 的状态/分类和 Tr 的状态/错误词按下载器显示。隐藏分支不把值误发给另一下载器，按契约明确清空规则。
- **操作与数据：** 保留 `get/update/delete_torrent_remove_task` 对应现有命令、`get_remove_torrents` 预览、`auto_remove_torrents` 执行。动作枚举 1 暂停、2 删除种子、3 删除种子及文件原样保留；预览基于已保存任务，未保存时明确需先保存，不假装预览草稿。
- **组件/逻辑：** `TorrentRemoveForm`、`RemoveCriteriaSummary`、`TorrentPreviewList`；QForm/QExpansionItem/QSelect。范围/单位校验，提示预览时间和执行时匹配项可能变化。
- **验收：** 三种动作区分明确；Qb/Tr 条件往返不丢；预览无写入、预览失败不误报零匹配；手动执行确认具体任务/动作；删除任务配置不表述为删除种子。

### PG23 自定义下载器

**源文件：** `src/views/download/UserDownloaderView.vue`。**路由：** `/userdownloader`。

- **现状与目标：** 保留用于刷流任务的独立下载器集合；导航归入设置，但不与 PG32 的全局下载器配置合并数据模型。
- **桌面：** 名称、类型、地址、保存目录、编辑/删除表格；新增/编辑 640px 左右，测试与保存两个明确按钮。
- **手机：** 下载器摘要列表，完整地址/目录可展开；全屏表单依次名称、Qbittorrent/Transmission、主机、端口、用户名、密码、保存目录。
- **操作与数据：** 保留 POST `/api/v1/brushtask/downloader/list`，`add_downloader` 通过 `test=1/0` 测试或保存、`delete_downloader` 删除。端口合法范围与必填验证；测试状态独立，测试成功不关闭表单或表示已保存。
- **组件/逻辑：** `UserDownloaderForm`、`ConnectionTestResult`，可与其他连接表单共用基础控件，不共用命令和字段映射。
- **验收：** 新增/编辑/删除/测试均走原接口；密码脱敏占位不回写；测试失败保留输入；不同集合的下载器不互相覆盖。

## 7. 媒体整理页面

### PG24 文件管理

**源文件：** `src/views/rename/MediaFileQuasarView.vue`。**路由：** `/mediafile?path=`。

- **现状与目标：** 将目录浏览、识别转移、名称测试、TMDB 查询、硬链接查询、字幕与删除拆成可维护模块，保留现有全部工具。
- **桌面：** 左侧约 240px 懒加载目录树，右侧当前位置/面包屑/路径输入/刷新和文件列表。主操作“识别转移”，文件行提供重命名及更多；硬链接工具保留目录级入口。
- **手机：** 取消常驻树，使用逐级目录列表；顶部上一级和可展开路径；目录点开，文件点开详情/操作。路径可手动输入与复制，原路径 query 直达功能保留。
- **转移表单：** 输入路径、输出路径、八种现有转移模式、媒体类型/TMDB ID、季、集数定位、最小大小等分组；TMDB 搜索作为同层子步骤，返回保留参数。提交前核对路径与操作影响；没有逐文件预览接口时只展示参数摘要。
- **其他操作：** 名称识别结果与 PG04 共用；单文件字幕下载、重命名、删除保持可达。硬链接结果按源文件分组，保留全选/取消/反选和选中删除，明确选中的是哪些路径。目录级与文件级动作有不同标题。
- **操作与数据：** 保留 `get_sub_path` 的 ONLYDIR、MEDIAFILE、SUBFILE 过滤和原路径语义；保留 `rename_file/delete_files/download_subtitle/rename_udf/find_hardlinks/search_media_infos` 及名称测试、进度查询。不要按浏览器所在 Windows 平台重写 NAS 路径。
- **组件/逻辑：** `DirectoryPicker`、`FileList`、`TransferForm`、`TmdbMatchPicker`、`HardlinkResults`、`NameTestPanel`；`useFileBrowser/useTransferTask` 分别负责浏览和任务生命周期。
- **验收：** 根路径/中文/空格/长路径/空目录/权限失败正确；切目录旧响应不覆盖；八种模式字段兼容；取消网络等待不宣称取消转移；硬链接批量删除逐项确认结果，测试使用专用目录。

### PG25 手动识别

**源文件：** `src/views/rename/UnidentificationQuasarView.vue`。**路由：** `/unidentification`。

- **现状与目标：** 未识别列表保留单条/批量选择、移除未识别记录及重新识别，复用 PG24 转移表单但保留入口差异。
- **桌面：** 表格包含源路径、转移方式、识别/更多；选中后页头出现数量、批量识别和删除记录。
- **手机：** 长路径展示文件名与父目录摘要，点开看完整；显式选择模式，不靠长按。识别全屏填写来源/目标、模式、类型、TMDB ID、季、集定位/详情/偏移、最小大小。
- **操作与数据：** 保留 `getUnknownList/delUnknownPath/renameUdf`；核对 `delUnknownPath` 的语义，删除记录不直接描述为删源文件。批量识别须说明同一套参数应用哪些对象，不能默默把不同媒体匹配成一个 ID。
- **组件/逻辑：** `UnknownMediaList`、共享 TransferForm、`BatchActionBar`；字段按此入口的实际 RenameUdfParams 映射，保留各字段空值。
- **验收：** 单条与批量对象正确；类型切换季集字段合法；部分失败保留未成功项；列表刷新不清空未提交的识别草稿。

### PG26 转移历史

**源文件：** `src/views/rename/HistoryView.vue`。**路由：** `/history`。

- **现状与目标：** 保留搜索分页、海报、跳文件管理、重新识别、恢复与文件删除；将“删除历史”之类含糊文案改成实际文件影响。
- **桌面：** 搜索/筛选 → 表格（媒体/季集、来源与目标路径、转移方式、时间、动作）→ 分页；路径可展开复制，海报异步补全不阻塞列表。
- **手机：** 小海报摘要 + 名称/季集/时间/转移方式；路径在详情展开；更多列出重新识别、恢复和不同删除动作。批量操作有明确选择范围。
- **操作与数据：** 保留 `getTransferHistory`、`reIdentification`、`restoreHistory`、`deleteHistory`，尤其 `del_source/del_dest/del_all` 三种文件影响；跳 PG24 保留目标 path。恢复仅对当前逻辑可恢复的移动类型开放，其他模式解释原因。
- **批量结果：** 现有 batchRestore 逐个请求后统一成功提示需改为逐条检查 retcode，展示成功/失败/未知数量，失败项可重试但先核对结果未知项。
- **组件/逻辑：** `TransferHistoryList`、`FilePathPair`、`HistoryActionMenu`、`BatchResultSummary`；图片缓存以媒体类型+ID 为键。
- **验收：** 三类删除确认写清源/目标；非移动不能误恢复；批量业务失败不报全成功；海报缺失不影响筛选分页和文件跳转。

### PG27 TMDB 缓存

**源文件：** `src/views/rename/TmdbCacheView.vue`。**路由：** `/tmdbcache`。

- **现状与目标：** 保留搜索、分页、单条改标题、删除、全清空和 TMDB 链接，调整信息密度。
- **桌面：** 查询栏 + 表格（索引、标题、TMDB、海报、动作）；“清空全部缓存”放单独危险操作，不能紧靠普通刷新。
- **手机：** 标题/索引摘要/TMDB 标识列表；详情显示完整索引、海报、链接；修改标题为短表单，全清空需说明作用范围。
- **操作与数据：** `getTmdbCache/modifyTmdbCache/deleteTmdbCache/clearTmdbCache` 保留；把现有元组返回转换为领域对象供两端复用，原 cacheKey 完整保留。
- **组件/逻辑：** `TmdbCacheList`、`CacheTitleForm`、确认组件；末页删除后校正页码。
- **验收：** 特殊字符索引不截断后提交；改标题不误换键；单条与全量删除范围清楚；图片失败、无匹配、末页删除均正常。

## 8. 系统设置页面

### 设置页共同处理

继续使用并改造 `useConfigForm`：维护服务器快照、当前分组草稿、dirty、loading、saving、testing；只提交本组键，不因打开其他组覆盖尚未保存的值。接口动态字段按原 schema 保留，隐藏高级配置不等于清空。

部分连接测试当前采用 `save(items, true)` 后再 `testConnection(command)` 的两步流程。保留顺序，联调确认 test 模式的持久化及运行时影响；界面不能显示“保存成功”，也不能未经验证保证服务端完全不变。多个测试不要交叉覆盖临时配置；测试结果应记录对应的输入快照，字段变化后标记旧结果已失效。

### PG28 基础设置

**源文件：** `src/views/setting/BasicQuasarView.vue`。**路由：** `/basic`。

- **现状与目标：** 保留系统、媒体、服务、安全、实验室五组和刮削、自定义 CSS/JavaScript、制作组编辑，拆分超长单文件但不丢键。
- **桌面：** 左侧组导航，右侧最大 960px 分组表单；当前组标题、说明和“保存本组”。组内长帮助改为短说明 + 可展开详情，常用字段靠前。
- **手机：** 五组入口列表或紧凑组选择器 → 单组全屏编辑；返回有未保存提醒；长脚本用全屏编辑区，底栏明确保存范围。
- **具体分组：** 系统包含日志、壁纸、Web 端口/账号/证书、代理/域名/User-Agent；媒体包含 TMDB、识别、分类、转移方式、最小大小/忽略词、命名格式、覆盖和刮削；服务包含签到/RSS/订阅搜索/媒体同步周期及监控/自动搜索；安全包含各来源 IPv4/IPv6 和 API Key；实验室保留现有六项实验开关。
- **子流程：** 刮削按元数据/图片及电影/电视剧/季/集分组；制作组保留分号与正则语义；CSS/JavaScript 保留保存接口，不在编辑预览中执行脚本。样式迁移可能使旧用户脚本选择器失效，说明自定义代码兼容范围，不能承诺自动适配。
- **操作与数据：** 保留源码各 `*_KEYS` 的保存边界，`getSystemConfig/save_user_script` 等既有流程；可能改变访问地址/认证的字段提交前明确影响，重启或重新登录要求以接口与实际行为为准。
- **组件/逻辑：** `BasicSystemForm/BasicMediaForm/BasicServiceForm/BasicSecurityForm/BasicLabForm`、`ScraperOptions`、`UserScriptEditor`、`ReleaseGroupEditor`；QForm/QExpansionItem/QTabs，复用密钥输入和分组保存栏。
- **验收：** 五组原字段逐键对照；仅保存当前组；其他组草稿不丢；刮削复选项、空配置、脚本换行和特殊字符原样往返；修改认证后能正确处理会话失效。

### PG29 自定义识别词

**源文件：** `src/views/setting/CustomWordsQuasarView.vue`。**路由：** `/customwords`。

- **现状与目标：** 保留按媒体分组的词条、批量启停、组/词 CRUD、导出、导入解析与勾选；将密集嵌套表格和多层导入弹窗拆开。
- **桌面：** 分组列表/折叠组 + 当前组词表，显示被替换词、替换词、偏移、定位词、类型、状态、备注；选中时显示批量启用/停用。导入采用“粘贴代码 → 解析 → 选择组/词 → 导入”同层步骤。
- **手机：** 先选组再看规则摘要卡；每条突出“匹配 → 替换/偏移”及状态。编辑全屏，定位/偏移字段按类型出现；导入选择为分组复选列表，表格不缩小到不可读。
- **字段与动作：** 保留 replaced/replace/front/back/offset/regex/season/type/enabled/help、组的 TMDB ID 与电影/剧集类型；保留组内全选、跨组已有选择、批量启停、导出与复制。选择键包含组和词的稳定身份，避免重名误操作。
- **操作与数据：** 保留 `get_customwords/get_custom_word/add_or_edit_custom_word/delete_custom_word`、组增删、`check_custom_words/export_custom_words/import_custom_words` 及既有导入解析命令。后端正则使用 Python 语义，前端不使用 JS RegExp 作为完全等价的强校验。
- **组件/逻辑：** `CustomWordGroups`、`CustomWordForm`、`WordImportWizard`、`WordExportPanel`、`useCustomWords`；分享内容仅作文本显示，不执行其中表达式。
- **验收：** 各词类型动态字段往返一致；含反斜杠正则不被转义破坏；导入只提交选中项；删除组说明包含的规则范围；导出复制失败有手动选择文本的回退。

### PG30 目录同步

**源文件：** `src/views/setting/DirectorySyncView.vue`。**路由：** `/directorysync`。

- **现状与目标：** 把源/目标/未识别目录与同步模式的关系讲清，保留行内启用和识别重命名开关。
- **桌面：** 列为源目录、目标目录、模式、识别重命名、启用、编辑/删除；路径对用上下排列和箭头表达，不靠很宽的两列承载完整路径。
- **手机：** 卡片显示“来源 → 目标”和模式/状态；主要启用开关可触达，编辑全屏，按目录/模式/选项分组。
- **操作与数据：** 保留 `get_directorysync/get_sync_path/add_or_edit_sync_path/delete_sync_path/check_sync_path`；源必填，目的空值的自动分类行为保留，未识别目录可选。两类开关提交中锁定当前控件，失败恢复服务器状态。
- **组件/逻辑：** `DirectorySyncForm`、`PathPair`、共享 DirectoryPicker/TransferModeSelect；八种模式的解释与 PG24 一致。
- **验收：** 自动目标与显式目标正确回填；切换 enable/rename 不串 flag；删除同步配置不误描述为删目录；长路径/不同模式/未识别目录为空均可保存。

### PG31 豆瓣同步

**源文件：** `src/views/setting/DoubanView.vue`。**路由：** `/douban`。

- **现状与目标：** 与探索的豆瓣电影/剧集页面明确区分，这是“同步配置”，保留历史查看/删除。
- **桌面：** 单页三组“账号与凭据”“同步范围与周期”“自动处理”，右上查看历史；历史为媒体/状态/时间表格。
- **手机：** 单列分组表单；历史全屏列表，保留回编辑草稿路径，历史删除放更多。
- **字段与数据：** 保留 users、days、interval、types、cookie、auto_search、auto_rss；用户 ID 可显示为多值输入，但提交仍按原逗号格式；types 用“想看/在看/看过”复选并映射 wish/do/collect。保留 `get_douban_history/delete_douban_history`。
- **组件/逻辑：** `DoubanSyncForm`、`DoubanSyncHistory`，QOptionGroup/QInput/QToggle；Cookie 遮挡、手动显示，不用于浏览器会话。
- **验收：** 多用户、三种标记、同步天数和周期正确；自动搜索/订阅独立保存；保存失败保留草稿；历史删除不影响文件，记录状态未知时不猜“已下载”。

### PG32 下载器

**源文件：** `src/views/setting/DownloaderView.vue`。**路由：** `/downloader`。

- **现状与目标：** 保留五种全局下载器、下载目录策略和播放限速；避免 UI 更新时只留下 Qbittorrent/Transmission。
- **桌面：** Qbittorrent、Transmission、115 网盘、Aria2、PikPak 连接卡片显示配置摘要/当前选择，点击配置；另设“下载目录”“播放限速”两个明确工具区。
- **手机：** 五种下载器用列表进入全屏配置；目录策略为按优先级排列的卡片，显示类型/分类/保存路径/容器路径/标签；若实现排序，提供上下移动按钮，拖拽仅为增强。
- **连接字段：** Qb 保留自动强制作种/自动管理；Tr 保留连接凭据；115 保留 Cookie；Aria2 保留端口/令牌；PikPak 保留账号/密码/代理。敏感字段统一遮挡。
- **目录与限速：** 保留目录匹配顺序、类型/二级分类、保存与容器路径及标签；限速保留 Qb/Tr 上下行、IPv4/IPv6 排除范围、带宽、剩余比例、分配比例。单位和 0/空禁用语义写在字段旁。
- **操作与数据：** 延续 useConfigForm、两步连接测试、`getSystemConfig/setSystemConfig` 对限速配置的处理；当前下载器切换及子表单分别保存，测试不误显示持久化成功。
- **组件/逻辑：** `DownloaderConnectionForm`、`DownloadDirectoryRules`、`PlaybackSpeedForm`，按实际重复项提取连接输入，不将全部类型压成无校验的任意 JSON 编辑器。
- **验收：** 五种类型字段完整；目录顺序不变；空/零和比例格式无损；测试的第一步失败时不继续第二步；手机能编辑全部限速和目录字段。

### PG33 下载设置

**源文件：** `src/views/setting/DownloadSettingView.vue`。**路由：** `/download_setting`。

- **现状与目标：** 将默认参数配置集合做成可读“下载预设”，页面正式名称可保留下载设置，避免与下载器连接页混淆。
- **桌面：** 表格列为名称、下载器、分类/标签、添加后动作、限速摘要、编辑/删除；布局与分享率/时间详细值放展开区。
- **手机：** 每个配置一张摘要卡，主要显示名称、适用下载器和添加后开始/暂停；编辑分“基本”“分类标签”“速度与做种限制”。
- **字段与数据：** 保留名称、下载器、分类、标签、上传/下载限速、布局、动作、分享率、做种时间。仅适用 Qb 的字段说明清楚，切换下载器不静默删除原配置。保留 `get_download_setting/update_download_setting/delete_download_setting` 及 sid。
- **组件/逻辑：** `DownloadPresetForm`、`DownloadPresetSummary`；QSelect/QInput/QChip。下载添加/订阅中使用同一预设选项加载能力。
- **验收：** 添加后暂停/开始枚举正确；标签保留 NASTOOL 等原值；数字空值不被转换成 0；删除后其他页面已选失效配置有提示，不悄悄替换。

### PG34 索引器

**源文件：** `src/views/setting/IndexerView.vue`。**路由：** `/indexer`。

- **现状与目标：** 保留内建、Jackett、Prowlarr 三种来源及连接测试；把内建站点选择做成可查找的选择列表。
- **桌面：** 来源卡片与当前配置标记；内建站点对话框显示搜索、全选当前可见项、已选数量，外部来源显示地址/API Key/Jackett 密码。
- **手机：** 来源列表 → 全屏配置；内建站点一行一选，筛选不会自动取消不可见的已选项，全选范围明确。
- **操作与数据：** 保留 `get_indexers`、pt.search_indexer 与来源配置键及两步测试。空站点列表区分未配置和加载失败；失效站点保留提示，保存前明确处理，不在 load 时静默丢弃。
- **组件/逻辑：** `IndexerSourceCards`、`IndexerSitePicker`、`ExternalIndexerForm`，QCheckbox/QList/QForm；选择值继续用现有站点 ID。
- **验收：** 三种来源选择正确写入；测试不更改 UI 已保存状态；全选/取消和已选数量一致；旧配置含失效站点可识别。帮助文案不链接不存在的站点管理路由，新增站点管理属于另行确认的范围。

### PG35 媒体库

**源文件：** `src/views/setting/LibraryView.vue`。**路由：** `/library`。

- **现状与目标：** 保留电影、电视剧、动漫、未识别四类目录，统一目录选择器与删除配置文案。
- **桌面：** 四类独立分区，名称、目录数量、添加目录；目录行可复制/移除；目录选择对话框左树右当前路径，支持手输。
- **手机：** 四类折叠组或分组入口，目录用换行列表；新增目录进入全屏逐级浏览，当前路径明确，底部“选择此目录”。
- **操作与数据：** 保留 `media.movie_path/tv_path/anime_path/unknown_path` 和 `updateDirectory` 的 add/sub；目录树通过 `get_sub_path` ONLYDIR 懒加载。区分从配置移除目录与删除 NAS 文件。
- **组件/逻辑：** `LibraryDirectorySection` + 共享 DirectoryPicker + PathDisplay；手工路径与浏览选择共用校验、提交。
- **验收：** 四类目录可独立增删；重复/空路径处理合理；根目录与无权列目录可手动返回；移除配置确认不谎称已删除实体目录。

### PG36 媒体服务器

**源文件：** `src/views/setting/MediaServerView.vue`。**路由：** `/mediaserver`。

- **现状与目标：** 保留 Emby/Jellyfin/Plex 的类型专属字段及当前服务器选择，连接状态必须来自测试或实际请求。
- **桌面：** 三张来源卡，显示当前选择和配置入口；编辑为表单 + 独立测试结果，底部测试/保存。
- **手机：** 来源列表 → 单列表单；说明文字精简，密钥可临时显示，测试结果紧贴按钮而非仅 Toast。
- **字段与数据：** Emby/Jellyfin 保留 host/api_key，Plex 保留 host/token/servername/username/password；`media.media_server` 和各来源配置键不变。保留 useConfigForm + 两步 testConnection；保存后使概览相关缓存过期，重新按权限加载。
- **组件/逻辑：** `MediaServerForm`、`ConnectionTestResult`，QCard/QForm；切换来源先保存/放弃当前草稿，不复制上一来源密码到新来源。
- **验收：** 三种配置字段完整；Plex 不被简化成只有 API Key；错误地址/认证失败/测试超时可区分；保存失败不更改当前服务器标记。

### PG37 消息通知

**源文件：** `src/views/setting/NotificationView.vue`。**路由：** `/notification`。

- **现状与目标：** 保留源码 CHANNELS 中全部 11 种渠道、通知项、交互/启用开关、测试与自定义消息；将动态表单从列表中提取。
- **桌面：** 表格列为名称、渠道、推送内容摘要、交互、启用、编辑/测试/更多；“发送自定义消息”作为独立次操作。表单分“基本信息”“渠道参数”“通知内容”。
- **手机：** 渠道摘要卡显示名称/类型/启用/通知数量；交互状态进入详情；编辑全屏，通知项分组复选，支持全选/反选并显示数量。
- **渠道保留：** Telegram、微信、Server酱、Bark、PushDeer、PushPlus、爱语飞飞、Slack、Gotify、Chanify、Synology Chat。每种保留现有字段、默认值、必填与条件项；API Key/Token/Secret 等改为敏感输入。外部平台配置帮助在实施时再核对，不把旧说明直接认定为最新规则。
- **操作与数据：** 保留 `get_message_client/update_message_client/test_message_client/check_message_client/delete_message_client/send_custom_message`；改变类型需明确丢弃/保留旧渠道草稿，不能混发配置字段；开关逐条锁定，失败回退。
- **自定义消息：** 标题、图片 URL、正文在同一表单，发送前说明实际目标范围；若接口不支持指定渠道，不新增虚假的收件渠道选择。测试可能实际发消息，按钮说明“发送测试消息”，仅用户点击后触发。
- **组件/逻辑：** `NotificationClientForm`、`NotificationSwitchPicker`、`CustomMessageForm`、`NotificationClientList`；现有 schema 提取为独立配置文件，QInput/QSelect/QToggle/QOptionGroup。
- **验收：** 11 种渠道字段逐项映射；交互仅适用于支持的渠道；自定义/测试消息不因页面初始化发送；发送失败不清正文；启用失败不保留错误开关状态。

### PG38 字幕

**源文件：** `src/views/setting/SubtitleView.vue`。**路由：** `/subtitle`。

- **现状与目标：** 保留 OpenSubtitles、ChineseSubFinder 两种服务选择，清晰展示本地与远端路径映射。
- **桌面：** 两种服务摘要卡，配置入口；ChineseSubFinder 表单按连接/目录映射分组，路径示例说明替换方向。
- **手机：** 服务列表 → 全屏表单；映射按“本地路径 → 字幕服务路径”纵向显示，两条路径都可完整查看。
- **字段与数据：** 保留 `subtitle.server`，OpenSubtitles 的 enable，ChineseSubFinder 的 host/api_key/local_path/remote_path；路径一致时可按原语义留空。此页源码只有保存流程，连接测试要有真实接口后才能添加，不能套用其他页面的测试按钮。
- **组件/逻辑：** `SubtitleServiceForm`、`PathMappingFields`，QCard/QForm/QToggle；保存成功后关联文件字幕动作使用最新配置。
- **验收：** 切换服务无字段串写；路径为空/一致/不同均正确回填；修改配置不自动下载字幕；图标或服务图片缺失有文字回退。

### PG39 用户管理

**源文件：** `src/views/setting/UsersView.vue`。**路由：** `/users`。

- **现状与目标：** 保留用户列表、新增、删除和现有权限项；当前没有编辑用户/重置密码的完整页面流程，不把它们当现有功能迁移。
- **桌面：** 用户名、权限摘要、删除表格；新增为用户名/密码/权限分组表单，权限说明可展开。
- **手机：** 用户卡显示用户名及权限数量，展开查看全部权限；新增全屏，权限用带文字的复选项，保留默认“资源搜索/探索”意图。
- **操作与数据：** 使用 `get_users/user_manager` 的原 payload；权限文案可以调整，但保存值保持源码 ALL_PRIS，包括当前路由不存在的“站点管理”。该权限标明对应能力可用性，不擅自创造页面或丢掉已有权限值。
- **权限行为：** 管理页与添加/删除动作按真实权限开放；当前账号、默认管理账号是否可删由后端规则与会话身份确认。没有身份依据时不猜测目标；后端拒绝要展示明确原因。
- **组件/逻辑：** `UserList`、`CreateUserForm`、`PermissionPicker`；密码仅存在于当前表单，提交成功/退出后清理。
- **验收：** 管理员新增/删除、普通用户拒绝访问、用户名冲突、权限为空、后端拒绝删除均有正确结果；不新增不可用的重置密码入口。

### PG40 过滤规则

**源文件：** `src/views/setting/FilterRuleQuasarView.vue`。**路由：** `/filterrule`。

- **现状与目标：** 保留规则组、默认组、规则增删改、模板、分享、导入和恢复初始规则；用可解释的规则摘要降低复杂度。
- **桌面：** 左侧规则组及默认标记，右侧当前组规则表/列表，显示名称、优先级、包含/排除摘要、大小、促销；组操作与单条操作分别放置。
- **手机：** 组列表 → 组内规则卡 → 全屏编辑；默认组有文字，分享/导入/恢复进入独立步骤，不多层弹窗叠加。
- **表单安排：** 基础名称/优先级；包含与排除多行表达式；大小限制与促销。提示优先级数值越小越优先，大小单位及剧集单集折算采用经验证的原语义。保留换行与逻辑关系，不把表达式随意拆成标签。
- **操作与数据：** 保留 `get_filterrules/filterrule_detail/add_filterrule/del_filterrule`、组增删、`set_default_filtergroup/share_filtergroup/import_filtergroup/restore_filtergroup`；模板应用保留现有字段映射。恢复前说明所选初始组和经确认的覆盖范围，不能假装可撤销。
- **组件/逻辑：** `FilterGroupList`、`FilterRuleForm`、`RuleSummary`、`RuleSharePanel`、`RuleRestorePicker`；QForm/QExpansionItem/QList；保存后使订阅/搜索的规则选项缓存失效。
- **验收：** 多行/数组格式转换无损，促销值不只保存显示文案；默认组唯一状态正确；导入错误保留原内容；删除/恢复范围明确；已有订阅引用失效规则时不自动换默认。

## 9. 共享弹窗与非路由界面

这些界面需要与相关页面一起完成，不能遗留 Element Plus；同一表单由桌面和手机共用数据，不各写一份提交逻辑。

| 编号/源组件 | 具体修改 | 关联页面与验收 |
| --- | --- | --- |
| OV01 `src/components/AddDownloadDialog.vue` | 桌面 560–720px，手机全屏；区分搜索结果下载与手工新增。手工新增明确种子文件/磁力链接，文件列表支持移除、失败重试；下载设置 → 保存目录联动；搜索模式展示资源摘要。保留上传、download_torrent、download 等实际接口差异 | PG03/PG21；上传成功不等于任务添加成功；切换预设旧目录请求不覆盖新目录；取消不重复添加 |
| OV02 `src/components/AddRssMediaDialog.vue` | 桌面分组表单，手机同层步骤；媒体身份、季集、基础设置、质量/规则/站点/目录按需展开。保留编辑回填、模糊匹配、洗版等源码已有选项、已保存默认设置与继续添加语义；未选/全选站点按原契约区分 | PG03/PG06–PG17；电影隐藏季集，剧集校验；来源入口的媒体类型/ID/季正确；编辑详情未加载成功禁止保存 |
| OV03 `src/components/SeasonSelectDialog.vue` | 少量季使用大点击区域复选，很多季支持滚动；手机短面板或全屏；“选择 N 季”明确提交范围，返回保留已选 | 媒体卡/详情/订阅；不把一个季和多个季的请求参数混用；取消不选默认季 |
| OV04 `src/components/RssMediaDetailDialog.vue` | 桌面只读详情，手机全屏；名称、类型/季、进度、规则/站点摘要；编辑/搜索/刷新/移除分级。共享状态和集数算法，详情→编辑采用同层替换 | PG15/PG16；移除成功同步列表及其他媒体订阅标记；失败不消失；点击编辑返回后信息更新 |
| OV05 `src/components/MediaCard.vue`、`src/components/PersonCard.vue` | QImg + 自适应布局，图片预留比例；媒体/人物分别呈现；文字可访问，卡片进入详情，内部操作阻止冒泡；无 hover 依赖 | 全部探索页与订阅入口；海报失败无塌陷，同名不同类型不串身份，手机/键盘可用 |
| OV06 `src/components/MessageCenter.vue` | 顶栏按钮；桌面侧面内容、手机全屏列表。保留实际消息字段和 refreshMessage 增量参数，追加去重，限制缓存；未读如仅是本地状态需明确定义 | 全局；会话退出清理、消息刷新失败不连弹 Toast；正文作为文本显示，未验证链接不变成任意业务动作 |
| OV07 `src/components/RealTimeLogs.vue` | 桌面大面板、手机全屏；保留来源/刷新，增加本地暂停跟随、换行、回底部、复制；打开才轮询，关闭清理；约 1,000 条受控上限 | 全局/PG04/长任务；用户上翻不被拉回底部；新日志提示，持续 10 分钟不无界增长；无日志与拉取失败区分 |
| OV08 `src/components/GlobalRequestProgress.vue`、`src/services/requestProgress.ts` | 请求总进度与后台业务进度区分；静默轮询默认不打扰。无法计算总量时用不定进度，取消/异常同样结束对应请求记录 | 全部；并发请求不会一个结束就提前归零，也不会失败后永久占线 |
| OV09 `src/components/GlobalOverlays.vue`、`src/stores/modal.ts` | 用 Quasar 反馈服务替换 ElMessage/ElMessageBox 和全屏遮罩；保留 confirm 的 Promise 返回语义。全局 modal 只用于确需阻断的确认，普通提交局部 loading | 全部；取消不成为异常 Toast；焦点恢复；不把网络下载百分比当媒体任务进度 |
| OV10 `src/components/HelpTip.vue` | 短说明贴近字段，长说明点击展开；桌面 tooltip 可增强，手机必须可点击。公开参数说明与高级配置键分层 | 全部表单；触屏可读、键盘可打开，正文不使用过小字；旧外部平台说明后续核对 |
| OV11 `src/components/PlaceholderPage.vue` | 核对引用；无引用则在 P6 清理，有引用先补充对应功能或明确不在范围 | 不得用占位页作为本次任何旧路由的完成结果 |

## 10. 跨页面同步规则

| 发生的操作 | 应更新的页面/状态 | 实施要求 |
| --- | --- | --- |
| 添加/编辑/移除订阅 | 电影/剧集列表、媒体卡片、媒体详情、相关日历 | 按媒体类型/ID/季与订阅 ID 更新或失效缓存，不能只改当前卡片 fav |
| 创建下载任务 | 当前下载表单、下载列表、相关搜索结果反馈 | 显示已受理及查看任务入口；后端未返回任务前不伪造百分比 |
| 修改下载预设/目录 | 下载确认、订阅编辑、自定义 RSS 下拉选项 | 正在编辑的值若失效明确提示，保留草稿由用户选择，不静默替换 |
| 修改规则组/识别词 | 搜索/订阅规则选项及涉及的配置摘要 | 显示失效引用；不擅自对正在运行的任务重发请求 |
| 切换媒体服务器 | 概览与相应媒体状态查询 | 使旧查询失效并重取，不能把上一个服务器的数据当作新服务器数据 |
| 完成文件转移/删除/恢复 | 文件列表、未识别列表、转移历史等相关区域 | 根据真实结果局部刷新；不用全站强制重载打断其他草稿 |
| 退出/会话过期 | 全部页面和全局弹层 | 停止轮询，清理账号相关缓存与敏感输入；登录回跳保留合理目标，不跨账号复用数据 |

临时表单与查询状态保留在稳定父层/composable/store 中，展示容器切换不会卸载唯一草稿。跨页面缓存遵循字段真实来源，不建立一份无法可靠同步的前端“全站数据库”。

## 11. 页面实施与验收交付模板

以下模板在未来实现每一页时填写；本次文档编写不勾选为已完成。

```text
页面编号/路由：
实施阶段：
涉及源文件和共享组件：
已迁移的原功能/字段/动作：
桌面、平板、手机的布局与状态证据：
接口样例与成功/失败判据（脱敏）：
新增的前端交互能力：
依赖后端确认的能力及降级方式：
关键验收场景和结果：
遗留问题、负责人及处理阶段：
```

每页完成条件：

- [ ] 该页对应 PG 条目、共同要求、关联 OV 条目均完成，不仅是组件标签替换。
- [ ] 旧路由/参数/字段/动作逐项比对；没有误加不可用功能或静默删除低频能力。
- [ ] 桌面表格和手机卡片读取同一查询结果，筛选/选择/保存逻辑共用；断点切换不丢状态。
- [ ] 390/768/1440px 和浅深色可用；返回、键盘、触控、软键盘、安全区检查通过。
- [ ] 实际接口的失败/未知/部分成功均正确呈现；高风险操作用专用测试数据验证。
- [ ] 类型检查、构建及与该页行为相关的测试通过；不为纯视觉替换编写镜像式测试。

建议实施顺序仍按 task.md 的 P2–P6：先外壳/认证及反馈，再概览/搜索/下载核心闭环，随后探索/订阅，最后整理/设置和依赖清理。PG 编号用于逐页追踪，不表示开发先后顺序。
